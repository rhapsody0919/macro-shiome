# macro-shiome (マクロ潮目)

**株式投資の判断に使う指標を 1 つの Web アプリに集約する**。段階を分け、**v1 = バリュエーション**
(S&P 500 / NASDAQ-100 の EPS・PER・イールドスプレッド・予想改定・理論値と割高率)、
**v2 = 市場サマリー** (日経平均・ゴールド・ドル指数・セクター別騰落率など)。
参考にする分析者の記事は転載せず、同じ指標を自分でデータソースから集めて表示する。

## 参考記事

- **泉さんの記事**: https://note.com/joyous_deer792/membership (「泉の日米経済、株式市場分析」。
  メンバーシップ限定の可能性があり、Claude からは本文を取得できないことがある)。
  ユーザーが「泉さんの記事を読んで、追加すべきものがあるか検討して」と指示したら、この URL を指す。
  記事は転載せず、指標のアイデア源としてのみ使う (#93〜#98 と同じ扱い)

## 前提・制約

- **完全無料スタック** (課金しない)。無料枠の制限 (GitHub Actions の実行時間、データソース API のレート制限、Cloudflare Pages のビルド回数) は設計制約として扱う
- **定期バッチ**: GitHub Actions が **UTC 毎日 02:00 (JST 11:00)** に実行 → データ取得 → **JSON をリポジトリに commit** → push が Cloudflare Pages のビルドをトリガ。値が動かない日は差分が出ず commit もビルドも走らない (#136)
- **公開リポジトリ**。NEVER: API キー・トークンをコード / コミット / ログ / クライアントバンドルに残す。秘密情報は GitHub Actions Secrets のみ。**public リポの Actions ログは誰でも読める**
- パッケージマネージャは pnpm

## 技術スタック

SDD Step 2 で確定 (詳細 → `docs/plan.md`、判断の記録 → `docs/adr/`)。

- データストア: **Git 内 JSON** (DB を使わない。ADR-0001)。**Supabase は不採用** — 無料枠が「1 週間の非活動で停止」で週次更新の本アプリと噛み合わないため
- フロント: **Next.js 静的エクスポート** + TypeScript + Tailwind (ADR-0002)
- ホスティング: **Cloudflare Pages** (ADR-0002)
- チャート: **Recharts** (ADR-0003)
- データソース: FRED API / FactSet Earnings Insight 週次 PDF / stockanalysis.com /
  **米財務省 Fiscal Data** (国債入札・平均利率・残高、ADR-0006) / **Finnhub** (ETF 終値、#128) /
  **統計ダッシュボード** (登録不要、ADR-0007) / **e-Stat 統計 API** (appId 必須、#160) /
  **Tiingo** (ETF の日足 OHLCV、ADR-0008) / **Robert Shiller 本人の公開データ** (シラーPER、ADR-0010) の 9 つ

## 命令

- コミット = `type(#N): 何をしたか [why: 理由]` (#N = 対応 Issue 番号)
- 技術判断 → `docs/adr/` に 1 ファイル。凍結 `inception/` は不可侵、変更は `.agent/decisions.md`
- 技術選定 = 複数案比較で推奨。候補 2 つ以上 / 公式ドキュメントで無料枠・コスト・整合性・運用負荷を確認 / 評価軸を表で / 各案の不適合点を明示
- 原因調査 = 推測禁止、行番号・パス・コマンド出力で示す
- **使い捨ての調査スクリプトも `scripts/_*.ts` に置く。** bash / python の一行野郎にしない。
  型チェックとリントが効くため、正規表現・存在しないフラグ・import の結合ミスがコンパイル時に落ちる。
  **終わったら消す** (Stop フックが残骸を検知する)
- **金融データの定義・計算式は一次情報 (データ提供元の定義) で裏取りする**。EPS / P/E / 益回りの定義や期間 (forward 12M か実績か、暦年か会計年度か) を推測で決めない。指標の出所と定義は ADR かコードコメントに残す
- **指標を追加・変更したら `pnpm verify` を通す** (#113)。実装が動くことではなく**表示している数値が正しいこと**を確かめる。値が間違っていても画面は正常に見えるため、「緑だが検証していない」状態がこのアプリで最も危険
- 末尾 `## STATE` を節目 (着手 / 実装完了 / push / 判断待ち) ごとに更新: (1) 進行中 Issue (#N) とタイトル (2) 進捗 (3) Next Action。進行中 + 直近完了のみ残し、2 世代以上前は `docs/state-archive.md` 先頭へ退避

## 開発フロー (省略禁止)

1. 課題発見 → 実装せず Issue へ
2. Issue 作成 (GitHub Issues、open) → 停止。NEVER: Issue 無し着手
3. ユーザーの着手指示を待つ
4. Issue に assignee を設定 (= In Progress、`gh issue edit N --add-assignee @me`)。NEVER: 指示なし着手
5. feature ブランチで実装 → typecheck / test / build 全グリーン。NEVER: スコープ外混入 / 指示なく別 Issue に着手
6. **セルフレビュー観点 (下記) を diff に自分で当てる。** Critical/High は即修正、Suggestion は修正 or 理由を記録
   - **`/code-review` は Claude から起動できない** (`disable-model-invocation`)。より深いレビューが要るときは
     **ユーザーが PR 作成後に `/code-review` を打つ**。Claude 側から代替実行や再現はしない
7. PR 作成。**1 Issue = 1 PR 厳守**。PR 本文に `closes #N` を記載 (マージで自動クローズ・相互リンク)。NEVER: main 直 push / 1 PR に複数 Issue / `closes #N` 欠落 / PR 本文・コミットに Claude Code 署名 (「Generated with Claude Code」「Co-Authored-By: Claude」)
8. **`/codex-review` は一時中断中** (2026-08-16、ユーザー判断)。PR 作成後は Step 9 のマージへ進む
   <!-- 再開する場合: `npm i -g @openai/codex && codex login` を実行し、以下を戻す。
   レビュー観点は AGENTS.md に、実行スクリプトは scripts/codex-review* に維持してある。
   8. `/codex-review` を実行 (Codex 第三者レビュー)。Codex が PR diff を read-only レビュー
   (観点 = ルートの `AGENTS.md`) → Claude が指摘ごとに修正 or 反論 (理由記録) → 再レビュー、を
   **Codex approve (Critical/High 0 件) に収束するまで自動ループ** (最大 5 ラウンド、超過は停止し
   ユーザー判断)。修正は必ず Claude が行う (Codex はコード不変更) -->
9. 収束 → **Claude が自動マージ** (`gh pr merge --squash --delete-branch`) → 後続 (Issue は `closes #N` で自動クローズ・`## STATE` 更新)
   - **マージ後 CI 確認 (必須、全マージ対象)**: main push で走るワークフローの結果を確認し報告に含める。完了までバックグラウンド監視でよい。失敗時は原因を分類: (1) infra 起因 (runner 停止・ネットワーク・5xx) → `gh run rerun --failed` を **1 回だけ** (2) 実失敗 (型 / テスト / ビルド) → 即修正 or Issue 起票。**rerun も失敗したら原因を再分類して Issue 起票**。NEVER: CI 結果未確認・失敗未報告のままセッション完了
   - docs コミットの扱い: `docs:STATE` を除く **docs コミット (`docs(...)`)** はユーザーが PR diff をレビュー → OK で Claude がマージ
   - **マージ後の完了報告に確認手順を必ず含める (毎回・省略禁止、ユーザーに聞かれる前に書く)**: 「マージ・CI 確認済み」だけで終わらせない。UI / 挙動変更を伴う場合は (1) URL (2) 画面 / 操作手順 (どこをどう操作するか具体的に) (3) 期待挙動 (何が見えるはずか) を書く。バックエンドのみ (UI 変更なし) の場合も「実機確認不要と判断した理由」を一言添える。NEVER: 手順を書かずユーザーに丸投げする
   - NEVER: セルフレビュー観点を当てずにマージ / 収束前 Done

- Goal 外の問題は触らず Issue 起票のみ (コミット非混入)、完了報告に「新規起票: #N」を併記
- 定期バッチ (GitHub Actions) を変更した場合は、次回の定時実行を待たず `workflow_dispatch` で手動実行して成否を確認し、報告に含める

### GitHub Issues

- 起票先: 本リポの Issues。**起票〜クローズは全て Claude Code (`gh` CLI) が行う**。人手の Web UI 起票は想定しない
- Issue 項目: タイトル (動詞含む) / 背景 / ゴール / 実装方針 (任意) / 受入基準 / 関連 Issue (任意) / 想定 PR タイトル
- ラベル: 種別 `bug`・`feature`・`improvement` + 優先度 `P1` (Urgent)〜`P4` (Low) + `owner-decision` (ユーザー判断待ち)
- 状態: open = Todo / assignee あり = In Progress / closed = Done
- 相互リンク: PR 本文の `closes #N` で自動化 (手動の URL 記載は不要)

### 指標を追加・変更したときの検証 (#113)

`pnpm verify` が機械的に見るのは次の 10 点。**実装と違う経路で照合する**のが要件で、
同じコードを通すと同じ間違いをする (FRED は API キーを使わない CSV 経路)。

1. 一次情報との照合 — 保存値を取り直して突き合わせる
2. 将来日付の混入 — 推計値を含む系列は末尾が未来になる (#100)
3. 最新値の範囲 — 全系列が指標マスタの範囲に収まるか (#102)
4. 時系列の順序と重複
5. 導出値の再計算 — ビュー生成の関数を呼ばずに計算し直す
6. 実質金利と TIPS 利回りの一致 (#115)
7. 下落率と最高値の整合 (#128)
8. ゴールドの終値と日付の照合 (#133) — 日付が明示された履歴ページと突き合わせる
9. **FRED の単位** (#212) — **`--units` を付けたときだけ回す**。系列ページは 1 件 79 KB で、
   項目 1 で既に 84 回 FRED を叩いている。連続アクセスは実測で弾かれるため日次バッチには
   入れない。単位は指標を足したときにしか変わらない

10. **最高値と 52 週高値の照合** (#224) — 積み上げた最高値が提供元の 52 週高値から
    離れすぎていないか。**閾値 10% は実測の最大 (4.29%) の 2 倍以上**

**桁の照合はネットワーク不要なのでテストにも入れてある** (`source-units.test.ts`)。
記録済みの提供元の単位と読者向けの表記を毎回突き合わせる。

**機械化できない観点は自分で見る。**

- **画面の説明文と実装の定義が一致しているか**。値が正しくても説明が誤っていることがある
  (#109: NASDAQ-100 表示中に「1 ÷ Forward P/E」と出ていたが実装は実績 PER)
- **系列 ID が一次情報のタイトルどおりか**。FRED のページを開いて確認する
  (#66: `RSCCASN` は小売売上のコントロールグループではなく衣料品店の売上だった)
- **比較している 2 つの値の時点が揃っているか** (#110: 割高率が 1 営業日ずれた終値と比べていた)
- **単位と桁が subtitle の記述と合っているか** (千戸・百万ドル・千件など)。
  **桁は 2 回間違えた** — #198 で `BUSLOANS` を百万ドル (実際は十億ドル)、#212 で自発的離職と
  採用を「人」(実際は千人)。どちらも指標マスタの `note` に誤った単位が書かれており、
  それを信じて写した。**note ではなく一次情報を見る**

### セルフレビュー観点 (Step 6)

- 変更した関数 / 変数 / 型の呼び出し元を全列挙
- regression リスク 3 つで影響なし確認 (時系列データの欠損・週境界 / タイムゾーン / UPSERT の冪等性 / JSON スキーマ互換 / 導出値の計算式)
- 影響画面を実機確認 (ダッシュボードの各チャート・相関サマリー・期間フィルター)
- テスト 1 件以上 / コメントと実装の乖離なし / try-catch の error 経路を通す
- 省略可: `docs/*.md`・`CLAUDE.md`・`.agent/*` のみ / typo 1 行 / フォーマットのみ。他 (`.ts` `.tsx` `.json` / SQL / CI) は必須
  <!-- 一時中断: `/codex-review` スキップ中のため、ループ記録を完了報告に残すルールは凍結 -->

### SDD

spec + screens → plan → tasks → 実装 (1 タスク = 1 PR)、各ゲートでレビュー。起動は `.claude/commands/` の `/step1-spec` `/step1-screens` `/step2` `/step3` `/step4`。

## STATE (2026-08-17 — **公開済み**。https://macro-shiome.o104085t.workers.dev)

設計フェーズ (SDD Step 1〜3) の経緯は `docs/state-archive.md` に退避済み。

### 完了 (23 件)

| 範囲 | Issue | 要点 |
| --- | --- | --- |
| 基盤 | #1 #2 #23 | Next.js 16 静的エクスポート。**TS 6 / ESLint 9 に意図的に固定**。CI |
| 取得層 | #3 #4 #5 | FRED / FactSet PDF / stockanalysis。**API キーはエラーにも出さない** |
| 検証・導出 | #6 #7 #8 | 1 件でも落ちたら全体失敗。導出値は実測値で検算済み |
| バッチ | #9 #10 **#136** | **日次 (UTC 毎日 02:00)** + バックフィル |
| 画面基盤 | #11 #12 | 期間フィルターを URL に載せる。鮮度・欠測表示 |
| チャート | #13〜#20 | バリュエーション 6 種 + サマリーバー + マクロ指標 |
| 規約 | #21 | 利用規約・出所ページ。出所一覧は指標マスタから自動生成 |
| デプロイ | #22 | **Cloudflare Workers Static Assets** に Git 連携で公開 |
| 改善 | #46 #52 #53 #54 | 基準益回りの内訳 / スプレッドの分布 / 欠測理由 / 蓄積中表示 |
| **指標拡充** | **#62〜#66 #72 #83 #84 #87** | 景気サイクルのセクション / 物価 / 住宅 / 労働・所得 / 小売 / LEI 完成 / 信用・商品・為替 / 新規求人 |
| UI/UX | #74〜#77 **#89** | 期間フィルター修正 / モバイル / サマリー / 情報設計 / **問いによる再編** |
| 保守 | **#58 #70** | 指数ラベル・切替 UI を 1 か所に集約 / favicon |
| **記事由来の拡充** | **#93〜#98 #100** | 10年債の理論値 / 地区連銀サーベイ / 家計の余力 / 国債入札 / 国際金利 / 交易条件 |
| 検証層 | **#102** | 範囲チェックが正常値で落ちる / ほとんど動いていなかった問題 |
| **全指標の値検証** | **#109 #110 #113** | 益回りの定義説明の矛盾 / 割高率の比較時点のずれ / **検証の再現可能化** |
| **取得済み指標の活用** | **#115〜#118** | TIPS で実質金利を検算 / 実績 P/E の平均 / 四半期の予想 / 日経平均 |
| **v2 市場サマリー** | **#118 #119** | 日経平均 / ゴールド (ETF 代替)。セクター別騰落率は保留 (#120) |
| 取得の時点 | **#125** | stockanalysis の値を「値が指す日」で保存する |
| **下落率の共通軸** | **#128 #131** | Stock Bot (GAS) の設計を移植。ETF 36 本を最高値からの下落率で横断比較 |
| 値の時点 | **#133** | ゴールドが 1 取引日ずれていた。取得元を Finnhub に一本化 |
| **日次化** | **#136 #140 #142 #138 #137 #135** | バッチを日次に。無意味な差分を除去。価格系列を日次グリッドで表示 |
| ETF 履歴 | **#147 #149** | 36 本に 50 営業日を取り込み。最高値の遡及を修正 |
| **日本の指標** | **#129 #152 #154 #155 #156** | 統計ダッシュボード API (登録不要)。`/japan` に景気動向指数・交易条件・雇用と所得・住宅 |
| **日本の拡充** | **#160 #162 #164 #170 #174 #180** | 街角景気 (e-Stat 統計 API)・消費者物価・鉱工業生産・家計の余力・対外収支・失業率と企業物価 |
| **日次化の完了** | **#166 #168** | 日次で取れる系列を日次グリッドに。日次ビューをページ別に分割 |

指標 178 件、テスト 1,258 件。`pnpm verify` で値を検証する (#113)。チャート 80 枚 (`/` 5・`/economy` 28・`/market` 23・`/japan` 24)。

**指数を増やすときの変更箇所は `src/lib/data/indices.ts` の 1 か所** (#58)。
`INDEX_LABELS` が 5 コンポーネントに複製され、切替 UI も実質 5 か所に散っていた。

### 画面構成 (2026-08-17 時点)

| ページ | 更新頻度 | 内容 |
| --- | --- | --- |
| `/` | 週次 | バリュエーション (指数 vs EPS / Forward P/E / イールドスプレッド / 理論値 / 予想改定 / 相関) |
| `/economy` | 日次 + 週次 + 月次 | 経済 20 チャート。景気は減速しているか / 雇用と所得は保つか / 消費は続くか / 物価は落ち着くか / 住宅は動いているか |
| `/market` | 日次 + 週次 + 月次 | 市場 23 チャート。金利はどこにあるか / 金融環境は緩いか厳しいか / 市場は不安か / いまどの資産が売られているか |
| `/japan` | 日次 + 月次 + 四半期 | 日本 24 チャート。日本の景気は減速しているか / 日本の物価は落ち着くか / 日本の雇用と所得は保つか / 海外との取引は円を支えるか / 日本市場はどう動いているか / 日本の住宅は動いているか |
| `/terms` | — | 利用規約・出所 |

**セクションは「問い」で分ける** (#89)。「先行指標」という見出しでは何に使うかが伝わらず、
指標が 55 件に増えた結果「その他」が最大セクション (旧 `/macro` の 82%) になっていた。
問いにすると「いつ何を見るか」がそのまま伝わり、上流 → 下流の並び順自体が読み方になる。
問いの定義は `src/lib/questions.ts`。

**更新頻度と景気サイクルはチャートのバッジに移した** (#89)。問いに答えるには週次と月次の
両方が要るため、頻度でページを分ける #64 の判断は撤回した。ただし
「先週から動いていない」のか「月次だから動かない」のかは判別できる必要があるため、
頻度をバッジで出す。分類の根拠は The Conference Board の構成要素のまま (#62)。
旧 `/macro` は `public/_redirects` で `/market` へ 301。

### 実装中に判明した重要な点

- **フィクスチャは実データの代わりにならない**。#4 で「ユニットテストは通るのに実 PDF で
  抽出できない」ことが起きた。以降、実データに対する統合テストを `*.integration.test.ts` に残す
- **系列 ID は必ず一次情報で確認する**。#66 で `RSCCASN` を小売売上のコントロールグループ
  として使いかけたが、実際は「**衣料品店の小売売上**」だった。推測で選ぶと別物になる
- **提供元の仕様は実データで確認する** (#53)。FactSet は翌暦年の予想を年央から載せ始める
- **判定は期間フィルター前の系列で行う** (#54)。フィルター後だと誤表示になる
- **長時間ジョブの実行中に PR をマージしない** (#10 のバックフィルで push が reject)
- **ブラウザ pane では画面下部の実機確認ができない**。`document.hidden: true` /
  `innerWidth: 0` で React がハイドレーションを遅延させる。Testing Library で代替する
- **Cloudflare のドキュメントと実 UI は食い違う** (#22)。Pages の Git 連携作成導線が無く、
  実際は Workers Builds に一本化されていた
- **複数セッションが同じ作業ディレクトリを共有すると、片方の `git checkout` がもう片方の
  未コミット変更を消す** (#240)。実際に別セッションが並行して別 Issue のブランチへ切り替え、
  こちらの未コミットファイルが作業ツリーから消えた (ブランチ自体は残る)。**コミット前の内容は
  他セッションの操作で失われうる**ため、複数セッションが同時に動きうる状況では
  `git worktree add` で作業ディレクトリを分離し、変更後は早めにコミットする

### データの制約 (画面に出している)

- **NASDAQ-100 の実績 PER は過去に遡れない**。QQQ 経由でしか取れず提供元が現在値のみ
- **EPS はすべて導出値** (指数 ÷ PER)。絶対値を無料で取れる経路が無い
- **住宅ローン申請者数 (MBA) は無料で取れない**。住宅ローン金利で代替 (#65)
- **小売売上のコントロールグループは FRED に無い** (#72)。建材・外食まで除いた系列が存在せず、
  自動車・ガソリンのみ除いた系列で代替。**季節調整済み系列の引き算では導出できない**
- **ISM 新規受注指数は FRED に無い** (ISM の著作権)。LEI の他の新規受注 2 系列で代替
- **中古住宅販売は FRED の提供が 13 点のみ**のため不採用
- **発表ラグは指標ごとに違う**。月次画面は系列単位で対象月を表示する

### 判断の原則 (繰り返し適用している)

- **恣意的な閾値を置かない**。VIX・イールドスプレッド・貯蓄率のいずれも「危険水準」の線を
  引かず、過去分布での位置を示す (#52)
- **根拠の無い分類をしない**。景気サイクルの分類は構成要素に無い指標には付けない (#62)
- **代替指標は「同等でない」と明記する** (#65)

### UI/UX (2026-08-17、#74〜#77 / #89 で改善)

実画面 (375px / 768px / 1280px) で調査し、5 件を修正した。

| 直したこと | 実測 |
| --- | --- |
| 期間フィルターが `/economy` で効かなかった (#74) | 121 点 → 13 点に絞られるようになった |
| モバイルでヘッダーが崩れる (#75) | 370px → **159px**。チャートが 1 画面目に入る |
| 各ページに「最新の状況」を追加 (#76) | 4〜6 チャートをスクロールせず現状が分かる |
| 見出し階層・系列色・欠測表示 (#77) | チャートを h3 に / 色を 1 か所に集約 / 正常な欠測は件数だけ |
| セクションを「問い」で再編 (#89) | 「その他」82% → 消滅。全 24 チャートが 7 つの問いに属する |

**画面の原則** (#89 で更新):
- ページは**対象または地域**で分ける (バリュエーション / 経済 / 市場 / **日本**)。
  地域で分けるのは #152 が唯一の例外。日本の指標は出所も単位も米国と違い、同じ問いに混ぜると
  「単位が違う」のか「動きが違う」のかを読み分けられない。**複数国を並べる図は分けない**
  (10年債利回りの国際比較・各国の下落率は「市場」に残す)
- セクションは**問い**で分ける。問いの中は上流 → 下流の順。
  **1 枚しかない問いを作らない** (#184)。細分化しすぎると問いが多いだけで読み筋が伝わらない。
  #89 が解いた「その他の肥大」の逆方向の失敗で、テストで固定してある
- **更新頻度と景気サイクルはチャートのバッジ**で示す (見出しからは外した)
- サマリーは**期間フィルターに依存しない**。常に直近を見せる。`/economy` は**問いごとに 1 指標**
- 月次のカードは**指標ごとに対象月を出す**。発表ラグが違うため

### 記事の探索結果 (2026-08-18)

参考記事 3 本 (債券バブル / リセッション分析 / 週次展望) を読み、未取得の指標を洗い出して
**#93〜#98 を起票**した。FRED / 財務省 API での存在は一次情報で確認済み。

**取れないことを確認したもの** (記録として残す):

- **NAHB 住宅市場指数** — FRED に無い (404、検索でもヒットせず)
- **NAR 仮契約住宅販売指数** — FRED に無い (`PHSI` 404)。NAR の著作権
- **S&P Global PMI (製造業・サービス・総合)** — 著作権で FRED に無い
- 既知: LEI (Conference Board) / MBA 住宅ローン申請 / ISM 新規受注 / 小売コントロールグループ

### 記事由来の拡充 (2026-08-18、#93〜#98)

| Issue | 追加したもの | 判断の要点 |
| --- | --- | --- |
| #93 #100 | **10年債の理論値** = 期待インフレ率 + 潜在成長率 | `GDPPOT` は**将来の推計値を含む**ため取得時に当日以降を切る。潜在成長率は 2.20% (CBO)。**着手前に CSV 末尾を読んで 1.78% と誤認**し #100 で訂正 |
| #94 | 地区連銀サーベイ (NY・フィラデルフィア) | ISM の穴埋め。**当月分が当月中に出る**唯一の月次指標。`unit: 'diffusion'` を新設 (0 を挟むため index の min 0 では弾かれる) |
| #95 | 個人貯蓄額・実質可処分所得の総額 | 率だけでは「取り崩し」か「所得減」か判別できない。実測で貯蓄額 −37.9% / 所得 +0.5% と乖離 |
| #96 | 10年債入札の応札倍率 | **4 つ目のデータソース** (米財務省、ADR-0006)。3 条件で絞らないとリオープンが漏れるか TIPS が混ざる |
| #97 | 日米独の 10年債利回り | 3 か国とも同じ定義 (OECD) に揃える。日次は米国しか無い |
| #98 | 交易条件 (輸入物価 vs 輸出物価) | 物価の連鎖には足さない。上流ではなく別の話のため |

**系列の末尾＝最新とは限らない** (#100)。推計値を含む系列では末尾が将来になる。
値を読むときは日付も一緒に見る。#66 の `RSCCASN`、#96 の TIPS 混入と同じ「別物を掴む」型の失敗。

### 取れないことを確認したもの

- **NAHB 住宅市場指数** — FRED に無い (404、検索でもヒットせず)
- **NAR 仮契約住宅販売指数** — FRED に無い (`PHSI` 404)。NAR の著作権
- **S&P Global PMI (製造業・サービス・総合)** — 著作権で FRED に無い
- 既知: LEI (Conference Board) / MBA 住宅ローン申請 / ISM 新規受注 / 小売コントロールグループ
- **ADP 雇用者数 (`ADPWNUSNERSA`)** — FRED で **DISCONTINUED**、最終 2026-06-13 (#176)。
  取得していたが画面に配線されないまま残っていた。増えない指標を残すとバッチが毎回
  「取れた」ことになり本当の変化と区別できないため外した (#131 の GXG と同じ)

### 検証層の教訓 (#102)

**「緑だが検証していない」状態が長く続いていた。** 範囲チェックは基準日 (金曜) の値しか
見ておらず、月次・四半期・入札と、発表が 1 週遅れる週次系列 (信用状況) が丸ごと対象外だった。
検証対象を「各系列の最新値」に変えた結果、財政収支・信用状況・応札倍率の 3 つが
**実測値で範囲外**と判明した (負になる指標に `min: 0` が課されていた)。

**単位だけでは自然な範囲が決まらない。** 応札倍率と PER はどちらも `ratio` だが 2 と 20。
指標マスタに `range` の上書きを持たせ、根拠を `note` に書く。

過去の観測をすべて検証すると 7 系列が範囲外になる (1971 年の 357 円、1980 年の FF 金利 20%、
2020 年のマイナス原油など)。**いずれも実在した値**なので範囲は広げず、最新値のみを対象にしている。

### 全指標の値検証 (2026-08-18)

**取得値・導出値は全件一致。** 実装を経由しない独立経路で照合した。

| 対象 | 件数 | 結果 |
| --- | --- | --- |
| FRED 49 系列 × 直近 30 点 (`fredgraph.csv` で取り直し) | 1,470 点 | 不一致 0 |
| 応札倍率 (財務省 API と照合) | 276 件 | 不一致 0 |
| バリュエーション / マクロ / 経済の全導出値 | 約 13,000 値 | 不一致 0 |
| 相関・分布・パーセンタイルの再計算 | — | 一致 |
| 非有限数 (NaN / Infinity) の走査 | 全ビュー | 0 件 |

**見つかったのは「値」ではなく「定義」の問題 2 件**だった。

- **#109**: NASDAQ-100 選択時に「1 ÷ Forward P/E」と「実績 PER」という矛盾する説明が並んでいた。
  実装は実績 PER 由来なので無条件側が誤り。指数ごとに出し分けるよう修正
- **#110**: 割高率が **1 営業日ずれた終値**と比べていた。EPS は FactSet 掲載の終値
  (発行日の前営業日) から作るのに、割高率はレポート日の指数と比較していた。
  実測で最大 2.7pt、直近でも 0.59pt のずれ。`fairValueBasis` を導入して時点を揃えた
  (割高率 3.97% → **3.38%**)

**FactSet の終値はレポート日の前営業日の値**であることを実データで確定した
(FRED の該当日と 0.000% 一致)。抽出は正しく、比較相手だけがずれていた。

### 取得済み指標の活用 (2026-08-18、#115〜#118)

取得しているのに画面にも検証にも使っていない指標が 5 件あった。

| Issue | 対応 | 判断の要点 |
| --- | --- | --- |
| #115 | `dfii10` を実質金利の検算に使う | **表示はしない**。FRED は `T10YIE = DGS10 − DFII10` と定義するため、導出した実質金利は恒等的に TIPS 利回りと一致する (5,909 点で最大差 0.000000)。チャートに出しても同じ線が重なるだけ |
| #116 | 実績 P/E と 5年/10年平均 | Forward と**重ねずに切り替える**。水準が違う (Forward 5年平均 19.9 / 実績 24.4) ので同じ軸だと「PER が上がった」と誤読される |
| #117 | 四半期の予想増益率 | 暦年と**重ねずに切り替える**。暦年は年内固定だが四半期は対象が入れ替わる |
| #118 | 日経平均 | 新しい問い「海外市場も同じ方向か」を作り USD/JPY を移した。S&P 500 とは水準を直接比べられないので指数化はしない (基準日が恣意的になる) |

**「重ねずに切り替える」が 3 回続いた。** 定義や対象が違うものを同じ軸に載せると、差が
「市場の変化」なのか「定義の違い」なのか判別できなくなる。#109 と同じ型の問題。

### v2 の到達点 (2026-08-18)

| 項目 | 結果 |
| --- | --- |
| ドル指数 | #84 で取得済み |
| 日経平均 | #118。新しい問い「海外市場も同じ方向か」を作り USD/JPY を移した |
| ゴールド | **#119。GLD (ETF) で代替**。FRED は 2022年1月に現物価格 (LBMA) の提供を終了 |
| セクター別騰落率 | **保留 (#120)**。FRED に無く 11 本の ETF スクレイピングになる。リクエストが 1 → 12 件 |

**ゴールドは現物価格ではない。** GLD 1 株は金 1/10 オンス相当だが、信託報酬 (年約 0.4%) の分だけ
長期では現物から目減りする。画面に明記している (#65 と同じ扱い)。

### スクレイピング系列の時点 (#125)

**現在値しか取れない系列は「いつの値か」でキーを決める。** 基準日 (金曜) 固定で保存していたため、
手動実行のたびに実行日の値が金曜の値として上書きされていた (実測で確認)。
`previousTradingDay` で実行日の直前の取引日をキーにする。定時実行 (UTC 土曜 02:00 =
米東部の金曜 22 時) なら金曜になり従来と同じ。

**実行日そのものは使えない。** 週次グリッドは金曜から過去に遡るので、土曜の日付で保存すると
金曜の点から見えなくなる。

この修正で **NASDAQ-100 の実績 PER とゴールドは次の定時実行 (土曜) まで表示されない**。
持っている値が月曜のもので、週次グリッドの最終点 (金曜) から遡れないため。
間違った日付の値を出し続けるより無い方を選んだ (#110 と同じ基準)。

### 下落率の共通軸 (2026-08-19、#128)

ユーザーが GAS + スプレッドシートで運用していた **Stock Bot** の設計を移植した。

```
下落率(%) = (最高値 − 現在値) ÷ 最高値 × 100
```

**水準が違う資産を同じ軸に載せるための正規化。** 金 (400 ドル) と S&P 500 ETF (770 ドル) と
原油 ETF (130 ドル) は水準では比べられないが、下落率なら並べられる。#118 で「S&P 500 と
日経平均は水準を直接比べられない」と諦めた問題の解でもある。

- **5 つ目のデータソース = Finnhub** (`Quote`)。ETF は FRED に無く、セクター指数も
  S&P DJI の著作権で取れないため、ここが唯一の無料経路
- ETF **36 本**。主要資産 9 / セクター 8 / コモディティ 7 / 各国 12
- Stock Bot の履歴 (2024-11-29 〜、金曜 89 週) を初期データとして引き継いだ。
  **起点は 2024-11-28 で史上最高値ではない**ことを画面に明記

**最高値はセルに上書きせず、観測から積み上げる導出値にした。** Stock Bot は
`Math.max` でセルを更新していたため、一度誤った高値を掴むと復旧できない。実際に
**VGT が 2026-04-21 に異常値を掴み、以降 85% で固定**されていた (同時期の QQQ は 3.8%)。
観測を全部残す形なら、異常な 1 点を消せば直る。

取り込み時に「**1 週で下落率が 20pt 以上悪化**」を汚染として弾く。逆向き (高い値 → 0) は
最高値の更新なので通す。

**GXG は対象から外した** (#131)。Finnhub が c=0 を返し、Stock Bot の履歴も 2025-11-22 で
途切れている。取得できないものを残すと週次バッチが恒久的に赤くなり、
**本当の失敗を見落とす** (「緑だが歯抜け」の裏返し)。

### フィールドごとに「値が指す日」が違う (#133)

**ゴールドは #119 以降ずっと 1 取引日ずれていた。** stockanalysis のページは
**当日の終値を「価格」として表示し、`Previous Close` はその 1 つ前**を指す。
定時実行 (金 22:00 ET、引け後) で取ると `Previous Close` = 木曜の終値なのに、
`previousTradingDay(土)` = 金曜のキーで保存していた。

履歴ページ (`/etf/gld/history/`) で二重に確定した。

| 保存キー | 保存値 | 実際は何日の値 |
| --- | --- | --- |
| 2026-08-18 | 405.49 | **08-17** |
| 2026-08-17 | 401.48 | **08-14** |

**`peRatio` はずれない。** PER はページの「価格」(= 当日終値) から算出されるため、
金 22:00 ET なら金曜の値でキーと一致する。**同じページでもフィールドごとに時点が違う**
のを #125 で見落とした。

対応: ゴールドの取得元を **Finnhub に一本化** (#128 で既に毎週取得しており当日終値と確認済み)。
stockanalysis から `previousClose` の経路を削除。週次のスクレイピングは 2 → **1 リクエスト**に。

**`pnpm verify` に「8. ゴールドの終値と日付の照合」を追加した。** 日付が明示されている履歴ページと
突き合わせる。障害注入で 2 通り (値を 1 日ずらす / 非営業日をキーにする) とも落ちることを確認済み。
**照合 0 件も失敗にする** (#102 と同じ「緑だが検証していない」を避けるため)。
キーのずれは `previousTradingDay` を共有する全 ETF に共通なので GLD 1 本で足りる。

### 日次化 (2026-08-19、#136〜#142)

**Stock Bot (GAS) の履歴 626 行は全曜日・間隔ほぼ 1 日だった。** 日次ヒストリカル API を
使っていたのではなく、**現在値スナップショットを毎日 1 行ずつ積んでいた**だけ。
macro-shiome との差は API ではなく**実行頻度**にあった。

**Finnhub の無料枠では日次ヒストリカルを取れない。** `/stock/candle` は Basic 以上
([公式回答](https://github.com/finnhubio/Finnhub-API/issues/546))。毎日実行して積み上げるのが唯一の無料経路。

| Issue | 対応 | 実測 |
| --- | --- | --- |
| #136 | cron を `0 2 * * 6` → `0 2 * * *`。`weekly.yml` → `daily.yml` | GitHub Actions は public リポで無料無制限。Cloudflare のビルドは 11.6 秒 |
| #140 | 値が変わらない観測ファイルを書かない | 90 ファイル → 2 ファイル |
| #142 | 同じ欠測の再検知で `gaps.json` を書き換えない | 2 ファイル → **1 ファイル 4 行** (`status.json` のみ) |
| #138 | 最高値を全観測から求める / Stock Bot 履歴を日次で取り込み | seed 89 点 → 500〜626 点 |
| #137 | 価格系列 6 チャートを日次グリッドで表示 | `/market` gzip 98 → 168 KB |
| #135 | ゴールドの履歴 50 営業日を取り込み、説明を実態に合わせた | GLD 1 点 → 50 点 |

**実行時刻 UTC 02:00 は動かさない。** 米東部の前日 21〜22 時 (引け後) にあたり、
`previousTradingDay` が「直前に閉じたセッション」を指す前提が保たれる (#133 の教訓)。

**時刻フィールドは差分を生む。** `generatedAt` (ビュー) / `updatedAt` (観測) /
`recordedAt` (欠測) はいずれも**読み手がゼロ**なのに毎回書き換わっていた。日次にした
ことで表面化した。`recordedAt` は意味的にも誤りで、再検知のたびに上書きすると
**「いつから欠測しているか」が失われる**。

**週次グリッドは「週足に丸める」実装ではない。** `fridaysBetween` は金曜の値だけを拾うので、
週内の高値・安値はそもそも図に無かった。同じ理由で **#138 の最高値がグリッド点の間の
高値を取りこぼしていた** (下落率が小さく出る方向のバグ)。

**マクロビュー全体は日次にしない。** `/economy` がサマリーのために macro を import しており、
実測で 425 KB → 1,348 KB に膨らんだ。日次で意味のある 9 系列だけ `market-daily.json` に
切り出した (`MacroPoint` からの `Pick` で型のずれを防ぐ)。

**ハイブリッド (直近 2 年だけ日次) は採らない。** チャート途中で点の密度が変わり、
それが変動の増加に見える。#116〜#118 と同じ「定義の違いが市場の変化に見える」型。

**グリッドを変えたら差分の意味も変わる。** 「変化」が前週比から前日比になるため、
`MacroChart` に `changeLabel` を**必須プロップ**として追加した。次に粒度を変えたときに
黙って通らないようにするため。

**「取り直せない」と書く前に、履歴が無いか確かめる** (#135)。#133 でゴールドに
「過去に遡って取り直せない」と書いたが、`stockanalysis.com/etf/gld/history/` は
サーバー側 HTML で**直近 50 営業日**を返していた。#109 と同じ「値は正しいが説明が誤り」の型を
自分で作った。説明には「取れない」ではなく**どこまで遡れたか**を数字で書く。

**バックフィルは one-shot、定期取得は変えない。** `pnpm verify` の 8 はこの履歴ページを
**取得実装と違う経路**として使っている。定期取得もそこに変えると同じ経路で自己照合になり、
検証が意味を失う。取り込み時は既存値との食い違いで落とす (#133 の再発検知)。

### 日本の指標 (2026-08-19、#129 / ADR-0007)

**FRED は日本の指標をほぼ提供していない。** 系列ページのタイトルと CSV の最終行で 20 系列を確認した結果、
住宅は `HOUSTJP` が**存在せず** (546 行返るが中身はエラーページの HTML)、
`JPNPERMITMISMEI` は 2008 年で DISCONTINUED、CPI は 2021 年、鉱工業生産は 2024 年で停止。
現行なのは失業率・輸出入額・10年債利回り・実質実効為替・実質 GDP の 6 系列だけ。

**行数だけで存在を判定してはいけない。** #66 (`RSCCASN`) と同じ型の失敗になるところだった。

**`dashboard.e-stat.go.jp` は `api.e-stat.go.jp` と別物で登録不要。** 公式に
"available to everyone without registration" と明記されている。**キーを持ち込まない**ことを
選んだ (公開リポなので秘密情報を増やすほど露出面が広がる)。appId が要る統計 API は、
ダッシュボードに無い詳細表が必要になったときに検討する。

**1 回のレスポンスに複数の系列が混ざる** (#96 と同じ型)。総戸数の例で月次 1,044 / 四半期 174 /
年 43 / 年度 43、原数値 608 / 季調値 696。`cycle` と `isSeasonal` の**両方**を固定しないと
同じ月に 2 つ値が返る。日付は `YYYYMM00` で年度は `2025FY00` の形なので**形式でも弾く**。

**単位が違うものは同じ図に載せない。** 日本の住宅着工は**戸**、米国 `HOUST` は**千戸**。
定義 (季調済年率換算) は同じでも図は分ける。総戸数 (年率換算) と内訳 (季調値そのもの) も
水準が 1 桁違うので分けた。

`pnpm verify` は**点数も見る**。値だけ比べると、絞り込みが外れて同じ月に複数値が入る事故を見逃す。

### 日本ページ (2026-08-19、#152)

**ページを地域で分けた唯一の例外。** 他は対象 (バリュエーション / 経済 / 市場) で分けている。
日本の指標は出所も単位も米国と違い、同じ問いに混ぜると「単位が違う」のか「動きが違う」のかを
読み分けられない (#116〜#118 と同じ型)。e-Stat から日本の指標を増やす方針なので、
分散が広がる前に分けた (ユーザー判断)。

移したのは**日本単独の 4 枚だけ**。日経平均・USD/JPY (`/market` の「海外市場も同じ方向か」) と
新設住宅着工 2 枚 (`/economy` の「住宅は動いているか」)。所属チャートが 0 になった
`overseas` の問いは削除した。

**国際比較の図は動かさない。** 「10年債利回りの国際比較」(日米独) と「各国の下落率」は
複数国を並べること自体が目的で、日本ページに置くと何を見る図か分からなくなる。

**ビューは分けなかった。** `economy.json` は 160 KB で日本ぶんは 12 KB しかなく、
切り分けの複雑さに見合わない。ページ重量で効いているのは `market-daily.json` (696 KB) の方。

### e-Stat からの拡充 (2026-08-19、#154〜#156)

記事から導き出して実装した枠組み (#93〜#98) を日本に写して探索した。すべて統計ダッシュボード
API (登録不要) で取れた。**e-Stat の appId は取得済みだが、今回は使っていない。**

| 枠組み | 日本での対応 | 実測 |
| --- | --- | --- |
| LEI の穴埋め (#62 #94) | **景気動向指数 先行・一致・遅行** | 498 点、2026-06 |
| 交易条件 (#98) | **輸出物価・輸入物価 (円ベース)** | 799 点、**2026-07** |
| 家計の余力 (#95) | **実質賃金指数** | 174 点、2026-06 |
| 新規求人 (#87) | **新規求人倍率・有効求人倍率** | 762 点、2026-06 |

**米国で取れないものが日本では取れる。** The Conference Board の LEI は著作権で FRED に
無く #62 で構成要素から代替を組んだが、日本は内閣府の景気動向指数を無料で取得できる。

**基準年が古い系列は停止している。** 実データの最終月まで見ないと掴み損ねる。

| 系列 | 2010年基準 | 2015年基準 | 現行 |
| --- | --- | --- | --- |
| 景気動向指数 | 2018-09 | 2023-04 | **2020年基準** |
| 鉱工業生産 | 2018-08 | 2023-04 | **2020年基準** |
| 輸出/輸入物価 | 2016-12 | 2022-04 | **2020年基準** |

CPI だけ **2025年基準**が現行で、基準年は系列ごとに違う。

**季節調整の有無で桁が変わる系列がある。** 実質賃金 (現金給与総額) は賞与が入るため、
2026-02 が原数値 82.1 に対し 2026-06 は 144.1。季調値なら 97.0 と 98.5。

**取れなかったもの** (記録として):

- **景気ウォッチャー調査 (街角景気)** — 検索 0 件。当月中に出る点で #94 の地区連銀サーベイに相当
- **日銀短観 業況判断DI** — 検索 0 件
- **第3次産業活動指数** — 検索 0 件
- **家計調査の月次消費支出** — 家計消費指数は 2017-12 で停止。実質賃金で代替 (同等ではない)

上の 3 つは e-Stat 統計 API (appId 必須) 側にある可能性がある。必要になったら確認する。

### e-Stat 統計 API の併用 (2026-08-19、#160)

**景気ウォッチャー調査は統計ダッシュボードに無い** (検索 0 件)。ADR-0007 で「ダッシュボードに
無い詳細表が要るとき」と書いた条件に初めて該当し、**7 つ目のデータソース**として
e-Stat 統計 API (appId 必須) を併用することにした。**ダッシュボードにあるものは
ダッシュボードから取る**方針は変えない。

| | ダッシュボード | 統計 API |
| --- | --- | --- |
| 認証 | 不要 | **appId 必須** |
| 単位 | 系列 | 統計表 |
| 絞り込み | `cycle` / `isSeasonal` | `tab` / `cat01` / `cat02` |
| 日付形式 | `YYYYMM00` | **`YYYY00MMMM`** |

**日付形式が違うのが罠。** 取り違えると全部ずれる。相手の形式を渡すと `null` を返すことを
テストで固定した。

**統計 API は appId をクエリ文字列に載せる。** URL を例外に出すとそのまま漏れるため、
`safeQueryLabel` で `statsDataId` と軸だけを出す。ダミーキーで実際に叩き、
エラーに appId が出ないことを確認済み。

**キー無しで照合できる経路が無い。** FRED はキー不要の CSV 経路で照合しているが統計 API には
それが無い。`pnpm verify` は同じ実装を通すが、**軸の絞り込みが崩れれば落ちる**。
キー未設定は飛ばさず失敗させる (#102 の教訓)。

### 指数の桁数が導出を左右する (2026-08-19、#162 #164)

**日本の指数は小数 1 桁しか公表されない。** そこから前年同月比を導出すると公表値とずれる。

| | 指数の小数桁 | 前年同月比 |
| --- | --- | --- |
| 日本の CPI | **1 桁** (101.7) | 導出 1.4970% / **公表 1.6%** |
| 米国 `CPIAUCSL` | 3 桁 (332.813) | 導出で問題なし |

#162 は Issue に「米国と経路を揃えて指数から導出する」と書いたが、**実装して値が食い違った**。
経路の統一より**公表値と一致すること**を優先し、公表の前年同月比系列に切り替えた。
#109 (値は正しいが説明が誤り) の逆で、**説明どおりに実装したら値が誤りになる**型。

#164 (鉱工業生産) では**実装前に桁数と公表値の有無を確かめた**。公表の前年同月比が
存在せず照合相手がいないため、導出せず**季調値の水準**で出した。

**指標を足すときは、まず桁数と公表値の有無を見る。**

### 日次化の完了とビュー分割 (2026-08-20、#166 #168)

**取得は日次なのに表示が週次**の系列が残っていた。下落率 36 本 (セクター別騰落率を含む)・
イールドカーブ・信用スプレッド・新規求人。

**下落率は日次にして軽くなった。** 週次グリッドは 2016 起点だが seed の開始は 2024-11-28 で、
524 点のうち非 null が 89 点 (17%) しかなかった。日次にして起点を seed に合わせると
**450 点・非 null 99%・1,298 KB** (週次は 1,486 KB)。存在しないデータの枠を運ぶのをやめただけ。

**ビュー JSON はページに丸ごとインライン展開される。** `market-daily.json` は 943 KB あり
3 ページすべてに載っていたが、実測すると**利用は 3 ページで重複ゼロ**だった。

| ページ | gzip (分割前 → 後) |
| --- | --- |
| `/market` | 251 → **214 KB** |
| `/economy` | 164 → **97 KB** |
| `/japan` | 134 → **73 KB** |

系列の割り当ては `src/lib/data/daily-series.ts` が唯一の定義。**重複をテストで固定した** —
同じ系列を 2 つのビューに入れると片方だけ直して食い違う。

### 「取れない」と書く前に調べ直す (#170)

#156 で「家計調査の月次消費支出は 2017-12 で停止しており使えない」と書いたが**誤り**だった。
停止していたのは `家計消費指数` で、**家計調査そのものの系列は現行**。実質消費支出・
実質可処分所得・平均消費性向がすべて取れた。

これは #135 (ゴールドに「過去に遡って取り直せない」と書いたが履歴ページがあった) と同じ型。
**「取れない」は調べ尽くした結論としてしか書かない。**

直近の値が #95 の狙いをそのまま示している。

```
実質可処分所得  +2.5%
実質消費支出    −3.3%
```

所得は増えているのに使っていない。片方だけでは「取り崩し」か「所得減」か判別できない。

### 実行時刻が「値が指す日」を決める (2026-08-20、#172)

日次バッチを **ET 19:41 (引け後)** に手動実行したところ、**ETF 36 本すべてで観測が 1 日ずれた**。
`previousTradingDay` が常に前営業日を返す実装で、UTC 02:00 の定時実行 (= ET 前日 21〜22 時)
を前提にしていたため。

| 実行 (ET) | Finnhub の `c` | 旧実装 | |
| --- | --- | --- | --- |
| 前日 21:00 (定時) | 前日の終値 | 前日 | 一致 |
| **同日 19:41 (引け後)** | **同日の終値** | **前日** | **1 日ずれる** |

`lastClosedTradingDay` に置き換え、**ET の 16:00 を境に判定する**。夏時間があるため
UTC からの引き算では判定できず `Intl` を使う。

**`pnpm verify` の 8 (#133) が検知してバッチを赤にした。** 日付が明示された履歴ページと
突き合わせる設計が、想定と違う原因のずれも捕まえた。「キーのずれは全 ETF に共通なので
GLD 1 本で足りる」という #133 の判断も、実際 36 本すべてが同じずれ方をしたことで裏付けられた。

**生成物のコンフリクトは main 側を採って作り直す** (#174)。日次バッチが `data/` を更新するため、
作業が長引くとビュー JSON が衝突する。手で解決せず `rebuild-views` で再生成する。

### 日米で構成が違ってよい (2026-08-20、#178)

#170 で日本に「家計の余力」(実質可処分所得 vs 実質消費支出) を入れたが、**米国側に実質消費支出が
無かった**。#95 で「率だけでは取り崩しか所得減か判別できない」と学んだのに、米国は所得側しか
持っていなかった。`PCEC96` を足して対称にした。

**平均消費性向は米国に入れない。** 日本には入れたが、米国は貯蓄率を既に持っており
消費性向はほぼ `100 − 貯蓄率` の恒等関係。#115 で `dfii10` を「実質金利と恒等的に一致するため
表示しない」と判断したのと同じ。**日米で構成が違うのは、既に持っているものが違うため。**

直近は逆を示していた。

| | 実質可処分所得 | 実質消費支出 |
| --- | --- | --- |
| 米国 | +0.52% | **+2.54%** |
| 日本 | +2.5% | **−3.3%** |

米国は所得の伸びを超えて使い、日本は所得が増えても使っていない。

### バリュエーションページの点検 (2026-08-20)

このセッションで唯一触っていなかった `/` を点検した。**問題なし。**

- 導出値 331 個を実装と別経路で再計算 → **不一致 0**
- 説明文は #109 #110 の修正が反映済み
- FactSet の 2026-08-14 が未取得だったが、**PDF が実際に 404** で提供元の休刊。取得は正しく、
  `publication-break` として記録されている
- NASDAQ-100 の実績 PER が 0 点なのは #125 の既知の挙動。QQQ の PER 履歴は
  `/statistics/` が 404 で**本当に取れない**ことを再確認した (#135 の教訓を適用)

### 日米の突き合わせで穴を見つける (2026-08-20、#178 #180)

**指標の一覧を日米で突き合わせると、問いの中核に空きが見つかる。**

| 見つかった穴 | 対応 |
| --- | --- |
| 米国に実質消費支出が無い | #178 で `PCEC96` を追加 |
| 日本に完全失業率が無い | #180 で追加 (求人倍率と逆方向に動く) |
| 日本に企業物価が無い | #180 で追加 (消費者物価の上流) |

**データソースは増やさない。** 日本の失業率は FRED (`LRHUTTTTJPM156S`) でも取れるが値が一致
するので、既にある統計ダッシュボードで揃えた。

直近が構図を示していた。

```
国内企業物価  +7.2% (2026-07)
消費者物価コア +1.6% (2026-06)
```

企業間の取引価格は上がっているが消費者に転嫁されておらず、企業が吸収している。
**企業物価は消費者物価より 1 か月早く出る**ため、物価系で最も速い。

### 全指標の再検証 (2026-08-20、指標 126 件)

前回の全件検証 (#113、指標 67 件) 以降に 59 件増えたので回し直した。**値の不一致は 0。**

| 対象 | 規模 | 結果 |
| --- | --- | --- |
| `pnpm verify` の 8 項目 (CI、appId 経路を含む) | 観測 187,873 点 | すべて通過 |
| 導出値を**実装を通さず再計算** | 4,491 値 | **不一致 0** |
| 範囲チェックを**全観測**に適用 | 187,873 点 | 範囲外 754 点 (すべて実在値) |

範囲外の内訳は #102 の記録と一致した。**新規追加した 59 指標は 1 件も範囲外を出していない。**

| 系列 | 点数 | 内容 |
| --- | --- | --- |
| `usdjpy` | 723 | 1971 年の固定相場 357 円台 |
| `fed-funds-rate` | 27 | 1980 年の 20% 台 |
| `savings-rate` | 3 | コロナ給付金時の急騰 |
| `wti` | 1 | 2020 年のマイナス原油 |

**検証スクリプトを書く側が間違えることがある。** 今回 2 度やった。

- 割高率を「理論値で割る」と書いて 163 件の不一致を出した。実装は `1 − 理論値 ÷ 指数値` が正しく、
  **コードコメントの定義を読んで自分の式を直した** (#113 で書いた「実装と違う経路で照合する」の
  裏返しで、別経路の側が誤ることもある)
- `checkRange` を引数 3 つで呼び、空配列を「非 null」と数えて全 187,873 点が範囲外という結果を出した

**見つかったのは値ではなくコメントの陳腐化 1 件** (#182)。`yield-spread.tsx` の JSDoc に
#15 実装時の実測値 (2.23%) が残っており、現在は 2.60%。主張は成立しているが数値が古かった。

### 画面の点検 (2026-08-20、チャート 52 枚)

#89 でチャート 24 枚のときに作った構造が、**52 枚**に増えた状態で成立しているかを測った。

| ページ | 枚数 | 問い |
| --- | --- | --- |
| `/` | 5 | (問い構造なし) |
| `/economy` | 20 | 5 |
| `/market` | 15 | 3 |
| `/japan` | 17 | 6 |

**#89 が解いた「その他が肥大する」問題は再発していない。** `financial` の 8 枚も
`slowdown` の 6 枚も、中身はすべてその問いに答えている。枚数の多さ自体は問題ではない。

**見つかったのは逆方向の失敗** (#184)。`/japan` に**1 枚しかない問いが 2 つ**あった
(他ページには 1 枚の問いは無い)。`jp-trade` (交易条件) と `jp-external` (経常収支) は
どちらも「稼いだ分が海外に流出しているか」で、私が #155 と #174 で別々に問いを立てたのが原因。

統合し、**交易条件 (価格) → 経常収支 (結果)** の上流 → 下流の順に並べた。
**「1 枚だけの問いを作らない」をテストで固定した。**

### FRED を機械的に走査して未取得を洗った (2026-08-20、#188〜#200)

**探索の方向を変えた。** これまでは #62〜#87 (LEI の構成要素)・#93〜#98 (記事由来)・
#154〜#180 (日米の突き合わせ) と、すべて**既にある枠組みの穴を埋める**方向だった。
「FRED に他に何があるか」から探したことが一度も無く、標準的な指標が漏れていた。

**取得済み 59 本に対し、現行かつ未取得が 26 本あった。正当な理由で外したものは 1 つも無い。**

| Issue | 追加 | 何が見えるようになったか |
| --- | --- | --- |
| #188 | 中古住宅販売 | **米国の住宅取引の大半**。FRED は 13 か月しか出さないので積み上げる |
| #190 | NFCI / ANFCI / STLFSI4 / DGS2 / SOFR | 「金融環境は緩いか」に**最も直接答える指数**を持っていなかった |
| #191 | 設備稼働率 / 耐久財受注 / 企業在庫 | **在庫循環**を見る指標が 1 つも無かった |
| #194 | コア CPI / コア PCE / 期待インフレ率 / 家賃 CPI | **FRB の物価目標そのもの**を持っていなかった |
| #196 | 継続受給 / U-6 / 参加率 / 就業率 / 採用 / 離職 | 失業率の改善が「職が増えた」のか「探すのをやめた」のか判別できなかった |
| #198 | FRB バランスシート / 10年-3か月 / 3か月 / 商工業貸出 | 信用の**価格 (スプレッド) は見ていたが量 (貸出) を見ていなかった** |
| #200 | 小売総合 / 自動車 / 在庫月数 / 一戸建て / NASDAQ総合 / ダウ / ユーロ / 人民元 | 総合とコアの差、需給バランス |

**単位が揃うものは既存チャートに足す。** 総合とコア、新規と継続、求人と採用と離職は
**差そのものが意味を持つ**ため重ねる。揃わないものは分ける。

**水準が桁違いの指数は前年同日比で揃える** (#200)。NASDAQ 総合とダウは 2 倍差で、
同じ軸だと片方が潰れる。指数化は基準日が恣意的になるので採らない (#118 と同じ判断)。
日次系列用の `yoyAsOf` を新設した。

**単位は一次情報で確認する。** `BUSLOANS` を百万ドルと書いたが実際は**十億ドル**だった (#198)。
同じ FRB のバランスシート系でも `WALCL` は百万ドルで揃っていない。

### 統計ダッシュボードも同じ方法で走査した (2026-08-20、#202 #204)

FRED に対して行ったのと同じ機械的な走査を日本側にも当てた。**取得済み 22 系列に対し、
月次・継続中・未取得が 40 本以上**あり、うち 11 本を取り込んだ。

**特徴は、米国側で入れたばかりの指標に日本側の対応物が揃っていたこと。**

| 米国 | 日本 |
| --- | --- |
| コア CPI | **コアコア** (日本の「コア」は生鮮のみ除く) |
| 在庫月数 | **鉱工業在庫率指数** |
| 設備稼働率 (%) | 製造工業稼働率指数 (2020=100) |
| 企業在庫 | 鉱工業在庫指数 |
| 商工業貸出 | 銀行貸出 |
| FRB バランスシート | マネタリーベース |
| 週平均労働時間 | 総実労働時間 |

**日本の「コア」は米国の「コア」ではない。** 日本のコアは生鮮のみ、米国のコアは生鮮 +
エネルギーを除く。**国際比較にはコアコアを使う。** 直近はコア 1.6% に対しコアコア 1.7% で、
エネルギーが物価を押し下げている。

**単位が同じでも水準の意味が違えば分ける** (#204)。稼働率は米国が % (長期平均 80% 前後)、
日本は 2020年 = 100 の指数。同じ「稼働率」でも比べられない。

**「0 件を失敗にする」検査が働いた** (#204)。総実労働時間を季調値で登録したところ取得時に落ち、
季節調整済みの系列が提供されていないことが分かった。黙って欠測にしない設計 (#129) の効果。

### 指標の解説を画面に出した (2026-08-20、#208〜#211)

**指標マスタの `note` は 161 / 171 件に書かれていたのに、画面に出る経路が無かった。**
パースするだけで、読んでいるコードが 1 つも無い。画面に出ていた説明は `page.tsx` に
手書きした 1 行だけで、指標マスタとは別管理。#109 (説明と実装の乖離) はこの構造から出た。

**`note` はそのまま出せない。** Issue 番号を含むものが 103 件、コード識別子を含むものが 20 件で、
開発メモが混ざっている。**宛先の違うフィールド**として分けた。

| | note | explanation |
| --- | --- | --- |
| 宛先 | 開発者 | 読者 |
| 形 | 自由記述 (25〜312 文字) | what / howToRead / seeWith の 3 項目 |
| 画面 | 出さない | 折りたたみで出す |

**単位・出所・系列 ID は手書きさせない。** `unitLabel` と `source` から自動生成する。
#198 で `BUSLOANS` の単位を書き誤ったのは説明文に埋め込んでいたため。

**「単位」ではなく「提供元の単位」と書く。** 月次チャートの多くは前年同月比 (%) を表示するが、
提供元の系列は指数や金額。「単位」とだけ書くと縦軸の単位と誤読される。

**月次は系列定義から辿る。** `MonthlySeriesDef.indicatorId` が必須なので、図に出ている指標を
そのまま列挙できる。**日次・週次の `SeriesDef` は指標 ID を持たない**ため
`explainIndicators` で明示しないと、3 本ある図でも 1 件しか説明されない。

画面に出る **121 指標すべてに解説が入った** (`/economy` 50・`/market` 34・`/japan` 37、重複除く)。
残る 50 件は下落率の ETF と検算用で画面に出ない。

**書き手 (将来の自分) が忘れても落ちるようにした。** 評価語 (#52 の原則) / 本文への単位 /
自分の系列 ID / 開発メモ / マークダウンの装飾を禁じ、**検査自体が落ちることを障害注入で確認**した。
実際に 3 回捕まえた (マークダウン 2 箇所、住宅着工の「千戸単位」)。

### 桁は 2 回間違えた (2026-08-20、#212)

**どちらも指標マスタの `note` に誤った単位が書かれており、それを信じて写した。**

| Issue | 指標 | 書いた単位 | 実際 |
| --- | --- | --- | --- |
| #198 | 商工業貸出 | 百万ドル | **十億ドル** |
| #212 | 自発的離職・採用 | 人 | **千人** (実データも 3,232) |

`pnpm verify` に 9 番目の検証を足し、提供元の単位を `sourceUnits` に 84 件記録した。

**日次バッチには入れない (`--units` のときだけ回す)。** 系列ページは 1 件 79 KB で、
項目 1 で既に FRED へ 84 回取りに行っている。**実測で連続アクセスが弾かれた** — 最初の一巡は
取れたが数分後に `fetch failed` になり、30 秒間隔で 3 回試しても回復しなかった。
毎日回すと**値そのものを照合している項目 1 を巻き添えで落とす**。

**桁の照合だけはネットワーク不要なのでテストで毎回見る**。記録済みの `sourceUnits` と
`unitLabel` の対応を突き合わせる。Percent や Index は日本語の表し方が一意に決まらず、
対応表を作ると表記ゆれで壊れるため対象にしない。

**FRED は経路によって振る舞いが違う。** `/data/<ID>.txt` はテキストではなく HTML を返す
(単位は系列ページの `series-meta-value-units` から取る)。

**「開発マシンから FRED に到達できない」は誤りだった** (2026-08-21 に訂正)。`curl` の既定
(HTTP/2) だけが `HTTP/2 stream 1 was not closed cleanly: INTERNAL_ERROR` で落ちており、
**`node` の `fetch` も `curl --http1.1` も 200 を返す**。`pnpm verify` は `fetch` を使うので
ローカルで全項目回る。**到達性を curl 1 つで判定しない。**

### うるう日 (2026-08-20、#206)

**前年日付を文字列で組み立てると、うるう日で翌日に飛ぶ。** `2024-02-29` から `2023-02-29` を
作るが存在しない日付で、`new Date` が **2023-03-01 にロールオーバー**する。`valueAsOf` は
過去にしか遡らないため、前年 3/1 の値を拾っていた (NASDAQ 総合で 41.41% → **40.47%**)。

指標 171 件で導出値 23,410 値を再計算して見つけた**唯一の不一致**。

### 画面の点検 (2026-08-20、チャート 77 枚)

前回 (52 枚) の点検以降に 25 枚増えた状態で構造が成立しているかを測った。

| ページ | 枚数 | 問い |
| --- | --- | --- |
| `/` | 5 | (問い構造なし) |
| `/economy` | 28 | 5 |
| `/market` | 21 | **3 → 4** |
| `/japan` | 23 | 6 |

**#184 で下限 (1 枚だけの問いを作らない) は固定していたが、上限が無かった。**
`/market` の「金融環境は緩いか厳しいか」が **12 枚**まで膨らんでも何も落ちなかった。
#89 が解いた「その他の肥大」の再来。**金利 6 枚を「金利はどこにあるか」に切り出した** (#219)。

金利は「いまどこにあるか」を読む図、環境指数や信用スプレッドは「緩いか厳しいか」を測る図で
**目的が違う**。同じ見出しに並べると上流 → 下流の読み筋が長すぎて伝わらない。

**上限 9 枚をテストで固定した** (現時点で最大の問い = `/economy` の「景気は減速しているか」に
合わせた)。全ページで見たかったが、重いページを 3 つ描画するとテストが 7 秒 → 107 秒になる。
**問題が起きた `/market` で押さえる。**

### 表示より細かい桁を保存していた (2026-08-20、#218)

**表示は 2 桁なのに、導出値が最大 18 桁の浮動小数で保存されていた。**

```
"drawdown": 1.1338509795855467   ← 保存
         1.13%                    ← 表示
```

下落率は seed (Stock Bot 由来) が 1〜2 桁で、**自分で導出した点だけが全桁**。日次で積み上げる
ため差は毎日広がる。読み手に届かない桁が、ページ重量と日次バッチの差分だけを増やしていた。

| ページ | 変更前 | 変更後 |
| --- | --- | --- |
| `/market` | 410 KB | **310 KB** |
| `/economy` | 178 KB | **139 KB** |
| `/japan` | 114 KB | **89 KB** |

**一律には丸められない。** 為替は 2 桁だと 1.1581 → 1.16 になり線が階段状になる。キー単位で 4 桁に上書きした。

**`valuation` だけ丸めていない。** `pnpm verify` の項目 5 が実績 EPS = 終値 ÷ 実績 PER を
相対誤差 1e-9 で再計算しており、**割り算なので丸めると差が残る** (222 に対し 0.04)。
通すには許容差を緩めるしかなく、検証で最も強い項目を弱めることになる。
**`macro` は丸めた** — 導出が足し算と引き算だけで、入力が既に 2 桁公表なので
再計算との差は 0.000000 のままだった。

**テストのタイムアウトを 20 秒にした** (#219)。ページ全体を描画するテストは 1 件で 6〜7 秒かかり、
既定の 5 秒では**実測 6,702ms で落ちた**。描画を伴うテストは 1 つの `it` にまとめる。

### 財務省 Fiscal Data を走査した (2026-08-20、#222)

**7 つのデータソースのうち、走査していたのは FRED (84) と統計ダッシュボード (33) だけだった。**
財務省は入札の応札倍率 1 件しか使っておらず、当たったら FRED に無い系列が 2 つあった。

| 追加 | 頻度 | 何が見えるか |
| --- | --- | --- |
| **国債の平均利率** (全体 / 短期債 / 中期債) | 月次 2001-01〜 | 発行済み国債が**実際に払っている**金利。3.45% (2026-07) |
| **国債残高 (市中保有)** | 日次 1997-09〜 | 32.27 兆ドル。市場が消化すべき量 |

財政について**収支 (量) と応札倍率 (需要) は見ていたが、利払い負担 (価格) と残高が欠けていた**。
10年債利回り (4% 台) が平均利率を上回っている間は、借り換えが進むほど利払いが増える。

**1 回のレスポンスに 16 系列が混ざる** (#96 の TIPS 混入・#129 の e-Stat と同じ型)。
`security_type_desc` と `security_desc` の**両方**で絞り、受信側でも行ごとに種別を照合する。
Non-marketable の Domestic Series は 7.577% で桁が違う。

**市中保有の内訳は 2005-04 以降しか報告されていない** (1993-04〜2005-03 の 2,958 日が null)。
提供の境界なので飛ばす — 応札倍率が 1999 年 11 月より前に無いのと同じ扱い。

`page[size]` の上限は **10,000** (20,000 は HTTP 400)。8,374 件が 1 回で取り切れる。

一次情報と **6,613 点を照合して不一致 0**。

### 日本の中古住宅は e-Stat 経由では取れない (2026-08-20)

米国の中古住宅販売 (#188) に相当する日本の指標を 2 経路で探した。**両方とも無い。**

| 経路 | 結果 |
| --- | --- |
| 統計ダッシュボード | 全 **5,822 指標**の一覧を取得して確認。「中古」「既存」で **0 指標** |
| e-Stat 統計 API | 「不動産価格指数」「既存住宅販売量指数」「既存住宅」で **0 件** |

**ダッシュボードのキーワード検索は完全一致しか効かない。** 部分語 (「住宅」) でも 0 件を返すため、
`getIndicatorInfo` を引数なしで叩いて全一覧 (4.0 MB) を取り、手元で絞った。

「中古住宅」で返る統計表は**すべて調査もの** (住宅市場動向調査の意識設問、5 年ごとの
住宅・土地統計調査) で、時系列で追える取引量・価格ではない。

相当する指標は 3 つとも e-Stat に載っていない。

- **不動産価格指数** (国土交通省、月次) — 国交省サイトでのみ公開
- **既存住宅販売量指数** (国土交通省、月次) — 同上
- **成約件数** (レインズ) — 民間団体で公的統計でない

取るなら国交省サイトからのスクレイピングで **8 つ目のデータソース**になる。
**`/japan` は着工ベースのままにする** (ユーザー判断)。米国側が中古を中心に見ているのに対し
日本側は着工しか見ていないが、**構成が違う理由はデータの有無**であって設計判断ではない (#178 と違う)。

### 走査していなかった 2 ソースを当たった (2026-08-21、#224 #226)

**7 つのデータソースの走査が全部終わった。** 残っていたのは Finnhub と e-Stat 統計 API。

#### Finnhub — 無料枠で使えるのは 2 つだけ

| | |
| --- | --- |
| `quote` / **`stock/metric`** | 200 |
| `stock/candle` / `forex/rates` / `index/constituents` / `etf/profile` | 403 |

`stock/metric` は **52 週高値を日付つきで返す**。**最高値は終値からしか積み上げていない**ため
(日次にしても日中高値は取れない)、実測で乖離があった。

| | 積み上げ | 52 週高値 | 開き |
| --- | --- | --- | --- |
| COPX | 95.70 | 99.99 | **4.29%** (最大) |
| GLD | 495.90 | 509.70 | 2.71% |
| SPY | 777.88 | 779.37 | 0.19% |
| 6 本 | — | — | マイナス = 52 週より前の高値を保持 (正常) |

**COPX の 4.29% は seed の履歴が金曜だけ**なことで説明がつく (日次化は #166 以降)。

**表示は変えず検証にだけ使う** (#115 で `dfii10` を同じ扱いにした前例)。積み上げ値と 52 週高値は
対象期間が違い、混ぜると「直近 52 週は提供元、それ以前は終値ベース」と**期間で基準が変わる**。
`pnpm verify` に項目 10 を足し、**閾値 10% は実測の最大 (4.29%) の 2 倍以上**に置いた。

#### e-Stat 統計 API — 日銀短観は無い、代替はある

**日銀は政府統計ではないので e-Stat に載らない。** 「短観」で返るのは個人企業経済調査
(2018 年度で終了)。代わりに**法人企業景気予測調査の BSI** (財務省・内閣府、四半期) を取り込んだ (#226)。

```
直近 (2026年4〜6月期)  大企業 -0.5 / 中堅 -3.9 / 中小 -17.6
```

**同じ景気でも規模で受け取り方が違う。** 街角景気は小売・飲食など現場で消費者に接する人の実感、
BSI は経営側。情報源が別なので重ねない。

**軸を列挙する形をやめた** (#226)。街角景気は 3 軸だが BSI は **5 軸**。出所は `axes`
(軸名 → 値のマップ) を持ち、受信側は**応答に出てきた軸をすべて見て 1 種類に絞れているか**検査する。
`cat05` はメタ情報の一覧で見落としており、データを取って初めて気付いた。

**旧表は 2019Q1 で停止していた** (#154 と同じ型)。検索で最初に返る `0003200132` ではなく
現行の `0003326000` を使う。

**四半期の日付コードは月次と同じ形。** `YYYY00MMMM` で、**月次は末尾 2 桁が繰り返し**
(`2026000707`)、**四半期は開始月と終了月** (`2026000406` = 4〜6 月期)。互いの形式を渡したら
null を返すことをテストで固定した。

**四半期を月次グリッドに載せると線が切れる。** 3 か月に 1 点しか無いため `connectNulls` を
**このチャートだけ**有効にした。構造的な欠測であって取得漏れではない。既定は繋がないまま (#53)。

### 更新履歴の画面表示 (2026-08-21、#240)

指標や機能を追加・変更しても画面上で気付く手段が無かった。`/updates` を新設し、
`data/changelog.json` (Git 内 JSON、手動保守) を新しい順に表示する。フッターに `/terms` と
並べてリンクを追加。

**保存形式は指標マスタの `note`/`explanation` (#208〜#211) と同じ「宛先の違い」を適用した。**
コミットログや Issue 番号をそのまま出す案 (自動生成) は開発者向けの粒度・表現が読者に漏れる
リスクがあるため採らず、常に読者向け文言のみを書く手動エントリにした。Issue 番号・コード識別子・
評価語・マークダウン装飾を禁じるガードレールをテストで固定 (`explanation.test.ts` と同型)。

**ヘッダー右上に未読バッジを追加した** (2026-08-21、#243)。フッターのリンクだけでは
スクロールしないと気付けないというフィードバックを受け、常時表示位置に「更新履歴」リンク +
未読バッジを追加。既読日は `localStorage` で管理し、`/updates` を開いた時点で既読にする。

localStorage の読み出しは素朴な `useState` + `useEffect` ではなく `useSyncExternalStore` を
使った。`react-hooks/set-state-in-effect` (エフェクト内の直接 `setState` を禁じる ESLint
ルール) に抵触したためで、外部ストア同期は本来この hook が対象とする用途そのものだった。

**静的エクスポートでは `usePathname()` が末尾スラッシュ付き (`/updates/`) を返す。**
既読判定を `pathname === '/updates'` で書いたところ、本番ビルドでバッジが消えない不具合を
実機確認で発見した (`pnpm dev` では再現しない)。自分のコンポーネントは両方を許容する形に
直したが、**既存の `NavLink` も同じ理由でアクティブ表示が本番ビルドで効いていない**ことが
分かった。スコープ外のため直さず **#245 を新規起票**した。

**#245 を修正した** (2026-08-21)。末尾スラッシュを無視して比較する `isCurrentPath`
(`src/lib/path.ts`) に切り出し、`NavLink` と `UpdatesLink` の両方をこれに統一。
`pnpm dev` では再現しないため、テストも開発サーバー相当・本番ビルド相当の両方の
`pathname` 値で固定した (`nav-link.test.tsx` は今回新規追加。従来テストが無かった)。

### Tiingo から ETF の日足 OHLCV を取り込む (2026-08-21、#229、ADR-0008)

カップウィズハンドル・ブルフラッグの判定には高値・安値が要るが、既存の下落率 (#128) は
終値のみ。無登録の Stooq (JS Proof-of-Work のbot対策) / Yahoo Finance (レート制限) は
実機で自動化不可を確認し、**8 つ目のデータソースとして Tiingo を採用した**。

セルフレビュー (8 角度 finder → 1 票 verify の自前ワークフロー) で 10 件の指摘のうち 8 件を
修正した: OHLCV 検証の 0 件ガード欠如 / 1 本の不正なバーで直近 10 日分が全滅する設計 /
統合テストの `toThrow(expect.not.stringContaining(...))` が実際には何も検証していなかった
(vitest は Error インスタンスそのものを matcher に渡すため常に pass する) / 429 のリトライ後
誤ったメッセージになる不具合 / ADR の見積もり数値と実装の食い違い / Tiingo ブロックの配置が
他の軽量取得を遅延させる問題 / `TIINGO_SYMBOLS` の手打ち重複 / `upsertOhlcvObservations` の
UPSERT ロジック重複。同じ `toThrow` の欠陥が既存の `finnhub.integration.test.ts` にもあり、
スコープ外として **#232 を起票**した。

**「1 時間 50 件」の解釈を誤っていた** (#237 で訂正)。瞬間のレート上限ではなく**カウント**の
上限で、対象 36 銘柄なら 1 回の実行では上限に当たらない。間隔を空けても消費は同じ 36 件で、
所要時間だけが 48 分に伸びていた。2 秒間隔・約 1.2 分に短縮し、429 の再試行待ち (1 時間枠の
使い切りなので数秒待っても回復しない) は別に 60 秒起点で持つよう分離した。

### High Tight Flag (ブルフラッグ) のパターン検出 (2026-08-21、#231)

William O'Neil の定義 (ポール: 8 週間以内に 100% 以上の上昇 / フラッグ: 3〜5 週間、
ポール高値から 25% 以内の下落) で判定し、`/market` の「いまどの資産が売られているか」
セクションに表示する。一般的な「ブルフラッグ」は形状の自由度が高く恣意的な調整が要るため、
恣意的な閾値を置かない原則 (#52) に沿ってこの定義を採用した。

**新しい問いは作らなかった。** #184 (1 枚しかない問いを作らない) のテスト制約に抵触するため、
下落率と対になる文脈が自然な既存の `drawdown` 問いに 1 枚として追加した。

**マージ時に #240 と軽微なコンフリクトが発生した。** 作業中に別セッションが並行して #240
(更新履歴機能) を実装・マージしていたことが判明し、作業ディレクトリの未コミット変更を誤って
巻き込みかけたため `git stash` で正しいブランチへ退避し直した。マージ自体は `loader.ts`/
`types.ts` の 2 箇所で import/export が競合しただけの意味的衝突なしで、両方を素直に統合した。

**カップウィズハンドル (#230) を実装した。** #231 と同じ #229 の OHLCV 基盤を使う。
検出ロジックで 2 件の設計判断が要った — (1) カップ底が左右のリムと同日になる退化ケースを
除外 (2) `findBestCup` は最小 weeks ではなく**最大 weeks (最も明確な大きなカップ) を優先**に
変更。テストが繰り返し失敗して発覚し、`scripts/_debug-cup.ts` (作業後削除) で原因を特定した。
U 字/V 字の形状条件は定量化する一次情報が無いため判定に含めていない (#231 と同じ判断)。

**Finnhub 統合テストの API キー漏洩検知が機能していなかった** (#232)。#229 で見つけた
`toThrow(expect.not.stringContaining(...))` の欠陥 (vitest は catch した Error インスタンスを
そのまま matcher に渡すため `StringContaining` が常に false → `.not` で常に true になる) が
`finnhub.integration.test.ts` にも存在し、`toSatisfy((error) => error instanceof Error && …)`
に置き換えた。

**更新履歴の未読バッジを追加した** (#243)。フッターのリンク (#240) は画面最下部でスクロール
しないと気付けず、更新の有無を受動的に確認する経路が無かった。`updates-link.tsx` の
`isUpdatesPage` ヘルパーを踏襲する形で判定を実装した。

**静的ビルドでヘッダーの active 表示が効いていなかった** (#245、bug)。`trailingSlash: true`
(ADR-0002) の本番ビルドでは `usePathname()` が末尾スラッシュ付きを返すが `pathname === href`
の単純比較では一致せず、常に非アクティブになっていた。**この修正は 2 セッションが並行して
着手し**、先に完了した側 (PR #248、`src/lib/path.ts` に `isCurrentPath` を切り出し
`NavLink`/`UpdatesLink` 両方で共有) が先にマージされた。自分の側の実装は差分が完全に重複して
いたため、ブランチを作り直さずローカル・リモートとも削除して破棄した。**複数セッション協調は
着手前の issue assignee 確認だけでは防げない**ことを示す事例として記録する。

### OHLCV のバックフィル (2026-08-22、#249)

**ADR-0008 の「遡及データを一度に大量取得すると 1 時間 50 件の制限にすぐ達する」という判断は
誤りだった。** 実機で確認したところ、`startDate` を過去に指定した**1 リクエストで複数年分が
一括で返る** (SPY で `startDate=2020-01-01` → 1,668 件が 1 レスポンスに収まった)。1 銘柄 =
1 リクエストなので、36 銘柄のバックフィルも 36 リクエストで完結する。#237 で訂正した
「1 時間 50 件はカウント上限であり瞬間レートではない」という教訓と同じ型の誤解釈だった。

`scripts/backfill-ohlcv-history.ts` (one-shot) で 2 年分をバックフィルし、36 銘柄すべてで
9 点 → 501 点に増加した。既存の直近観測値との食い違いは 0 件 (自作スクリプトで全銘柄照合)。

**カップウィズハンドルが 34/36 銘柄でほぼ同一パターンとして検出され、識別力への懸念が
出た。** バックフィル後に `/market` を実機確認したところ、対象 ETF のほぼ全てで
「カップ 65 週・ハンドル 1 週」という同じ形状が検出された。O'Neil のカップウィズハンドルは
「稀な」強気パターンのはずで、投資判断のシグナルとして機能していない可能性が高い。
バックフィル (#249) のスコープ外のため、検出ロジック (#230) の見直しとして **#251 を新規起票**した。

### カップウィズハンドルの検出条件を見直した (2026-08-22、#251)

一次情報 (StockCharts ChartSchool) を調査したところ、実装 (#230) に欠けていた条件が
2 つ見つかった。

1. **ハンドルの深さに上限がある。** "The handle can retrace up to 1/3 of the cup's
   advance, but usually not more." カップと同じ「直近上昇幅」を基準に、右リムからの
   下落率が 1/3 を超えたら除外する条件を追加した
2. **右リムはハンドル期間中の最高値であることを要求する。** 当初の実装は右リム候補の
   後に価格がさらに上昇していても無視しており、実質「上昇トレンド継続中」を「ハンドル」
   として誤検出していた。実データで確認したところ、検出された 34/36 銘柄中 **26 銘柄で
   この条件が破れていた** (例: 右リム候補 91.02 に対しハンドル期間の最高値が 103.25、
   13% も上振れ)

2 条件を追加した結果、**34/36 → 14/36 に絞られた。** 右リムの日付も従来は基準日から
機械的に固定 (全銘柄で同一日) されていたが、銘柄ごとに実際の局所高値の日付に分散する
ようになった。恣意的な閾値は追加していない (#52) — どちらも一次情報の定義、または
O'Neil の定義の論理的帰結 (右リムは「カップの右肩」であるべき) に基づく。

**それでも 14/36 (39%) はまだ多いように見える。** 実データを確認したところ「2026年8月
中旬に多くの ETF が高値をつけてから反落している」という実際の市場環境を反映している
可能性が高い。これ以上の絞り込みは新たな恣意的閾値の追加が必要になるため見送った。

### カップウィズハンドルのチャート描画とさらなる精度検証 (2026-08-22、#253 #254)

**チャートで実際に描画したら「カップの形をしていない」との指摘を受けた。** #251 後の
14 銘柄のチャートを実際に描くと、カップ底が左リムのほぼ直後 (期間の 1〜4%地点) に
偏っており、「左リム直後に一度下落し、その後ずっと上昇トレンドが続いているだけ」の
形だと判明した (#230 のコメントで「U 字型の形状条件は判定に含めない」としていた判断の
帰結)。一次情報をさらに調査し、2 条件を追加した。

1. **カップの深さ下限 12%。** IBD 公式資料 "THE 3 MOST PROFITABLE STOCK CHART
   PATTERNS" (investors.com/chartreading) の定義 (12-33%) に基づく。12% 未満は IBD の
   分類で Flat Base という別パターンになる
2. **左右の時間比率。** Martinelli & Hyman (1998, Stocks & Commodities 誌) は
   カップの左側 (下落) 20-120日・右側 (回復) 3-25日という非対称な時間配分を定義して
   おり、本文に "symmetry" という単語は一度も登場しない

**放物線フィット (R^2) による統計的検証で、左リム側の重大なバグを発見した。** 上記
2 条件を適用した実データを 2 次関数にフィットさせ「U字型らしさ」を定量評価したところ、
`VCR`/`MCHI` が統計的に「上に凸 (山型)」と判定された。原因は左リム候補の後にさらに
高い値 (例: VCR は左リム 357.57 のはずが期間中に 411.13 まで急騰) を無視していたこと。
#251 で右リムに入れた「ハンドル期間中の最高値であること」と対になる、左リム側の同種の
見落としだった。修正した結果、検出数は **34/36 → 14/36 → 2/36** まで絞られた。

**オープンソースの調査で「独自実装の試行錯誤」から方針転換した。** ユーザーから
「車輪の再発明を避け、オープンソースを見るように」との指示を受け、`stock-pattern`
(402 stars、GPLv3) を調査した。**このプロジェクトはカップウィズハンドルを対応パターン
から外している** (ヘッド・アンド・ショルダーズ / ダブルトップ / トライアングル / VCP
のみ対応)。実績あるオープンソースでも扱わないほど機械的検出が難しいパターンだと分かり、
**カップウィズハンドルはこの状態で保留とする** (ユーザー判断)。GPLv3 はコピーレフトで
macro-shiome には組み込めないため、コードは移植せず設計思想 (ピボット検出 → パターン
マッチング、許容誤差を固定比率でなく銘柄のボラティリティ基準にする) だけを参考にする。

**チャート描画 (#253) も方向転換した。** モックアップを作って試したが、日次データは
ノイズが強くカップの形が伝わりにくく、週足に変換しても改善は限定的だった。実例
(Tickeron) を調査したところ、商流のパターン認識サービスもチャート画像ではなく
「ステータス (Emerged/Confirmed) + 目標価格 + 的中率」のテーブル形式で見せていることが
分かった。**的中率は 42〜46% 程度** (Tickeron の実績) で、チャートパターン分析自体の
限界も分かった。今後は下落率ランキング (#128) を軸に、パターン検出をステータス列として
統合する方向に設計を変える。**ヘッド・アンド・ショルダーズ (逆三尊) / ダブルトップ・
ボトムは、ピボット検出ベースの機械的判定に向いているため優先実装の候補**。

### ピボット検出基盤とダブルボトム・逆三尊を実装した (2026-08-22、#256 #258)

**独自に条件を継ぎ足す設計から、ピボット検出 → パターンマッチングの体系的な設計に
切り替えた。** `pivots.ts` に `findPivots` (前後 N 本の中で未突破の高値/安値だけを
局所極値として抽出) と `averageBarWidth` (許容誤差の基準) を新設し、ダブルボトム・
逆三尊の両方がこれを土台にする。

**ダブルボトムは実データで劇的に絞り込めた。** Workflow (調査 3 エージェント → 実装
2 エージェント → 検証 2 エージェント) で作業し、検証エージェントが繰り返し欠陥を
発見した。

| 段階 | 検出率 |
| --- | --- |
| 初回実装 (基準日 1 点だけの範囲判定) | 36/36 (100%) |
| ネックライン未満の条件追加 | 22/36 (61%) |
| 2 番目の安値以上の条件追加 | 21/36 (58%) |
| C-基準日間の経路依存チェック | 11/36 (30.6%、うち 82% が別の欠陥) |
| **A-C 間の経路依存チェック追加** | **4/36 (11%)** |

**「ヘッド・アンド・ショルダーズ・ボトム (逆三尊) は Issue #256 が要求していない」と
検証エージェントに指摘され、実装を除外・別 Issue (#258) に切り出した。** #256 の
Workflow でダブルボトムと並行実装したが、#256 の本文は「ピボット検出基盤 + ダブル
トップ + ダブルボトム」の 3 点のみで、逆三尊への言及が無かった。CLAUDE.md の
「NEVER: スコープ外混入」「1 Issue = 1 PR 厳守」に抵触するプロセス違反で、
コード自体は退避してから正式に #258 として起票し直した。**Workflow のような
自動化された並行実装ほど、Issue のスコープ確認を怠りやすい**ことを示す事例。

**「車輪の再発明をしない」というユーザー指示を受け、Workflow で調査・実装・検証を
専門エージェントに分離した。** `BennyThadikaran/stock-pattern` (402 stars、GPLv3) 等の
実ソースコード (wiki ではなく) を調査し、数値条件のみを参照してコードは移植しない
方針を徹底した。GPLv3 はコピーレフトで macro-shiome (明示ライセンス無し) には
組み込めないため。

### 検出されたパターンにチャート表示を追加した (2026-08-23、#260)

#253 で「サインが点灯中 (検出された) 銘柄だけはチャートも表示する」というハイブリッド案に
合意していたが、#256 (ダブルボトム)・#258 (逆三尊) の実装に反映されないまま漏れていた
(テキストのリスト表示のみで、チャート描画のコードが一切無かった)。ユーザー指摘で発覚。

**新しいライブラリは導入していない。** 既存の Recharts 基盤 (`LineChart`/`ReferenceDot`/
`ReferenceArea`、#253 で検討済みの案 A) で `pattern-chart.tsx` を新設し、終値の折れ線 +
判定の根拠になった主要点 (ピボット) のマーカーを描画する。対象期間はパターンの起点から基準日
まで (`view.ts` の `slicePriceSeries` が検出時のみ切り出す)。カップウィズハンドル・
ダブルボトム・逆三尊の 3 つのリストコンポーネントに組み込んだ。0 件のときは従来通りテキストのみ。

**ブラウザ pane が below-the-fold の実機確認に阻まれた。** 既知の事象 (`document.hidden: true`)
がフレッシュなタブでも再現し、スクロール位置に関係なくタブ自体が hidden 扱いになっていた。
Testing Library で代替し、`ResizeObserver` をモックして Recharts に実サイズを渡すことで、
実データ (再生成済みビュー) でカップウィズハンドル検出 2 銘柄 × マーカー 4 点 = 8 個の
`ReferenceDot` が描画されることを確認した。

同じタイミングでユーザーから 2 件を追加で起票依頼された (着手はせず起票のみ)。

- **#261 ダブルトップ**: `double-bottom.ts` の高値・安値を反転した対称構造で実装できる見込み
- **#262 Tiingo の OHLCV バックフィル**: High Tight Flag はポール判定に 8 週間分の高値・安値が
  要るが、日次の積み上げ待ちのままだった。カップウィズハンドル (#249) と同じく one-shot で
  バックフィルすれば判定可能になる

### パターン検出をブレイクアウト検出に一本化した (2026-08-23、#264)

#260 でカップウィズハンドル・ダブルボトム・逆三尊の検出結果をチャート表示したところ、
ユーザーから「実際のチャートを見るとそのパターンの形に見えない」という指摘を受けた。
数値条件は満たしていても、それが人間が見て納得できる形を保証しないという、
ルールベース形状マッチングの構造的な弱点が可視化によって初めて露呈した。

振り返ると精度改善の過程自体がこの弱点を示していた。カップウィズハンドルは条件を
厳格化するたびに検出数が 34/36 → 14/36 → 2/36 と乱高下し (#251 #253 #254)、調査した
実績ある OSS (`stock-pattern`, 402 stars) はそもそもこのパターンを検出対象から
外していた。ダブルボトム・逆三尊も経路依存チェックを何重にも追加してようやく
実用的な検出数に収束した経緯があり (#256 #258)、業界の商用サービス (Tickeron) でも
的中率は 42〜46% 程度 (#254 で調査済み)。

**反転パターンの形状検出という方向性自体をユーザーと議論し、確立された定義を持つ
ブレイクアウト検出に転換した。** カップウィズハンドル (#230)・ダブルボトム (#256)・
逆三尊 (#258)・High Tight Flag (#231) を廃止し、N 日高値抜け (Donchian チャネル/
タートルズの短期システム、N=20 営業日) の単一の検出に置き換えた。**恣意的な閾値を
置かない原則 (#52) と相性が良い** — トレンドラインの目視引きのような恣意性が
入らない、業界で確立された定義を採用した。

実データで検証したところ、対象 36 銘柄中 **8 銘柄で検出**された (GLD/GDX/COPX/REMX/
CORN/EWU/VNM/THD)。全件で「上抜けた高値 < ブレイクアウト時の終値」の整合を確認済み。

**同じタイミングでユーザーから 2 件を追加で起票依頼された。**

- **#261 ダブルトップ**: 方針転換 (形状パターン検出の廃止) によりスコープ外になり、
  着手前に close した
- **#262 Tiingo の OHLCV バックフィル**: ブレイクアウト検出にも直近 20 営業日分の
  データの蓄積が要るため、引き続き有効

### ブレイクアウトチャートの表示期間を延ばした (2026-08-23、#266)

#264 リリース直後、ユーザーから「チャートが直近しか表示されず分かりづらい」との指摘。
`priceSeries` を「上抜けた高値の日 (`priorHighDate`) 〜基準日」で切り出していたため、
その日がブレイクアウト直前 (数日前) のことがあり、実データで GLD/CORN が**2 点しか
表示されない**事例があった。

`sliceRecentPriceSeries` (基準日から直近 90 営業日) に置き換えた。**表示専用の定数
(`CHART_DISPLAY_DAYS = 90`) は検出条件の `lookbackDays` (20) と別物**であることを
コメントで明記した — 判定に使うウィンドウと画面に見せる範囲は目的が違う。

**可視化して初めて気付く精度・可読性の問題が続いている。** #260 (形状が違う)・#266
(範囲が短すぎる) と、チャート化のたびに「数値としては正しいが読み手には伝わらない」
欠陥が見つかっている。新しい検出・表示を追加するときは、実装後に実データのチャートを
数件目視するステップを先に組み込む。

### ブレイクアウト検出をCHoCHベースに置き換えた (2026-08-23、#268)

#264 の N 日高値抜けは「直近 N 営業日の高値を上抜けたか」という機械的な定義で、
下落トレンドの構造そのものは見ていなかった。ユーザーから「下落トレンド中の前回の
高値を超えたかどうかを判定してほしい」との要望を受け、Smart Money Concepts (SMC/ICT)
の **CHoCH (Change of Character)** — 1つ目の安値→前回の高値→より低い2つ目の安値、
という下落構造の後に終値が前回の高値を上抜けたことを検出する定義 — に置き換えた。

**`joshyattridge/smart-money-concepts` (GitHub、MIT ライセンス) を参照した。** #256 の
`stock-pattern` は GPLv3 でコードを移植できずアイデアのみ参考にしたが、今回は MIT の
ためアルゴリズムをより踏み込んで参考にできた (コード自体は TypeScript のループ実装に
書き直し、移植はしていない)。

**スイング検出に「同じ向きの候補が連続したら極端な方だけ残す」後処理を追加した。**
`pivots.ts` (#256) には無かった処理で、高値→安値→高値→安値と厳密に交互になることを
CHoCH の判定 (直近3点の水準の並び) が前提にするため必要だった。

**`swingLength` は一次情報のデフォルト値 (50) を実データ検証したうえで採用した。**
5/10/15/20/30/50 を比較し、いずれも検出数が極端に偏らなかった (3〜6/36)。恣意的な
選定を避けるため、自分で数値を選ばず一次情報の既定値をそのまま使う判断をした。
実データでの検出 (4/36: QQQ, VFH, WEAT, EWG) はいずれも数か月スパンの妥当な構造だった。

**チャート表示範囲も同時に見直した。** `swingLength` が大きいと CHoCH の構造
(1つ目の安値) が直近90営業日 (#266 で決めた表示下限) より前になることがあるため、
構造全体と直近90営業日のうち広い方を表示するよう `slicePriceSeriesForChart` に変更した。

### PER に過去分布のパーセンタイルを追加した (2026-08-23、#271)

投資家×エンジニアの議論 (AI要約機能の検討で優先順位付けした5項目のうち優先度1位) で
合意。イールドスプレッドには過去5年分布のパーセンタイル表示 (#52) が既にあったが、
PER (Forward/実績) は基準線 (5年/10年平均) との比較のみで、「PERが18倍」が高いか安いか
分からなかった。

新規データ取得は不要で、既存の `percentileRank`/`distribution` (`derived.ts`) の
横展開で足りた。**`SpreadDistribution` 型と `buildSpreadDistribution` 関数を
汎用化した** (`HistoricalDistribution`/`buildDistribution`) — イールドスプレッド・
Forward PER・実績 PER の3系列で計算ロジックを共通化する。UI側も `DistributionPosition`
を共通コンポーネントに切り出し、フォーマッタ (`formatPercent`/`formatNumber`) を
差し替え可能にした。恣意的な閾値は置かず、パーセンタイルと平均との差という事実のみ
表示する (#52 の原則を踏襲)。

`pnpm verify` に検証を追加した (項目13)。**ビューには生成時点の日付が記録されていない**
ため、窓の正確な境界 (直近5年) を再現できない。代わりに「`latestDate` までの直近 `n`
個の非欠測値」を集める方式で独立に再計算した — 窓の中の非欠測値の個数がちょうど `n`
である以上、この集合は窓による集合と一致する。

新フィールド追加のため `pnpm rebuild:views` でビューを再生成した (観測値は変えず
導出値のみ更新)。NASDAQ-100 の実績PERは #125 の既知の制約で観測点が0点のため、
`trailingPeDistribution` は引き続き null。

### BOS (Break of Structure) を追加した (2026-08-23、#272)

CHoCH (#268) は「トレンド転換」だけを見ており、「既に上昇トレンドが継続している」
ことの確認シグナル (BOS) は #268 の相談時に対象外としていた。ユーザーから追加要望が
あり実装した。

**CHoCH と BOS は同じ構造抽出ロジックを共有する。** 直近の [1つ目の安値 (L1) →
戻り高値 (H1) → 2つ目の安値 (L2)] の3点構造から、終値が H1 を上抜けた最初の足を
検出する処理そのものは同一で、違いは L2 が L1 より低いか (CHoCH、下落継続中の反転) 高いか
(BOS、上昇継続) だけ。この共通部分を `structure-break.ts` に切り出し、`choch.ts`
(既存、挙動は変えていない) と新規 `bos.ts` がそれぞれトレンド方向の判定だけを行う。
`swingLength` は CHoCH で検証済みの値 (50) をそのまま共有し、BOS 用に別途チューニング
していない。

**実データ検証 (BOS 検出 16/36 銘柄) 中に `PatternChart` の重大な描画バグを発見した。**
Y 軸の表示範囲 (`domain={['auto', 'auto']}`) が終値の折れ線データだけから自動計算されて
おり、マーカー (`ReferenceDot`) が使う高値・安値がその範囲外だと Recharts が**サイレントに
描画しない**。SPY/VTI/VNQ で「1つ目の安値」マーカーが消えていた。**この不具合は #268 で
マージ済みの CHoCH 実装にも潜在していた**が、その時の検出結果 (4/36) ではたまたま
顕在化しなかった。終値・マーカー・水準線の全ての値から Y 軸範囲を計算するよう修正し、
回帰テストを追加した (修正前は実際にテストが落ちることを確認済み)。

**可視化のバグは検出母数が増えるまで発覚しないことがある。** #260 (形状が違う)・#266
(範囲が短すぎる) に続き、今回は「一部の値だけ描画されない」という新しい型の欠陥だった。
実データでの目視確認を、検出数が少ない機能から先に済ませても安心しない。

### セクター/ETFの相対強度 (対SPY) を追加した (2026-08-23、#274)

投資家×エンジニアの議論 (優先度2位) で合意。既存の下落率ランキング (#128) は
「最高値から何%下げているか」を横断比較できるが、下落が市場全体の要因なのか、
その資産固有の弱さなのかを区別できなかった。

**下落率と同じ Finnhub の生の終値を使い、SPY (S&P 500) との比率を最初の共通観測日で
揃えて以降の変化を%で示す。** OHLCV (Tiingo) とは別プロバイダ・別定義のため混ぜない
(#229 の判断を踏襲)。新しいルックバック期間などの恣意的な閾値は置かず、既存の
観測開始日をそのまま基準にした。

**SPY 自身は基準そのものと比べるため恒等的にゼロになる。** この不変条件を
`pnpm verify` に追加し (項目14)、他資産の再計算照合と合わせて検証する。
`drawdown.json` は表示の2桁に丸めてある (#218) ため、独立再計算との比較には
絶対誤差 (0.006) を使った — `differs()` の相対誤差はゼロ近傍の値で破綻する。

UI は `DrawdownChart` に「下落率/対SPY」の切替トグルを追加した (`forward-pe.tsx` の
Measure 切替と同じパターン)。**時系列の可視化 (ローテーション) を優先したため
`drawdown.json` が 1.3MB→2.57MB (gzip 96KB→168KB、+72KB) に増えた。** ランキング
(直近値のみ) に絞れば増加はほぼ無視できたが、投資家側が「スナップショットではなく
推移を見たい」と求めたための意図的なコスト。

**マージ時に他セッションの #272 (BOS) が並行してマージされていた。** 作業ディレクトリの
共有によるコンフリクトは発生しなかった (触ったファイルが被らなかったため)が、
#240/#245 と同種の並行作業が今回も起きたことの記録として残す。

### CHoCH のブレイクアウト検出に出来高フィルタを追加した (2026-08-23、#277)

投資家×エンジニアの議論 (優先度3位) で合意。CHoCH (#268) は終値の水準比較のみで判定
しており、出来高を伴わない (薄商いの) ブレイクアウトも検出していた。

**一次情報 (IBD、investors.com "THE 3 MOST PROFITABLE STOCK CHART PATTERNS") の
定義をそのまま採用した。** カップウィズハンドル・フラットベースの買いシグナルが
共通して要求する「ブレイクアウト日の出来高が平均比40%以上」という基準を使う。
#251/#254 でカップウィズハンドルの条件を見直した際も同じ情報源を引用しており、
本リポジトリで既に信頼している一次情報の再利用になる。平均の基準期間もIBDが
標準的に使う50日 (判定日を含まない) を採用し、恣意的な数値は追加していない。

**BOS (#272) は対象外にした。** CHoCH と BOS は `structure-break.ts` を共有するが、
出来高フィルタは議論の対象だった CHoCH にのみ適用し、`hasVolumeConfirmation`
(共有ファイルに追加) を `choch.ts` からのみ呼ぶ形にした。BOS は議論の時点で
存在しなかった別スコープの機能のため、適用するなら別 Issue で判断する。

**基準期間分の観測が無ければ検出しない設計にした。** 誤検出よりは検出漏れを選ぶ
(#102 と同じ原則)。実データ (2年分のバックフィル済み) では該当する銘柄は無かった。

**実データで検出数が絞られた。** `pnpm rebuild:views` で再生成した結果、
CHoCH検出は **4/36 (QQQ, VFH, WEAT, EWG) → 2/36 (VFH, WEAT)** になった。
QQQ・EWGは出来高を伴わないブレイクアウトだったことが分かる。

**テストの構築が難しかった。** 出来高確認には直近50日分の観測が要るが、既存の
波形ベースのテストフィクスチャは30〜150本程度しか無く、そのままでは出来高フィルタが
常に不合格になる。「価格の構造だけを先に (出来高を考慮せず) 判定してブレイクアウト日を
特定し、その日だけ出来高を跳ね上げる」という2段構成にして、既存テストの意図
(価格構造の検証) を壊さずに出来高フィルタを追加した。

### AI要約機能を追加した (2026-08-23、#279、ADR-0009)

投資家×エンジニアの議論 (優先度4位) で合意。「経済はどのような状態か」「注意が必要な
ことはあるか」「狙い目なセクター/ETFはあるか」に答える新規ページ `/summary` を追加した。

**LLM選定をやり直した。** 議論時点の第一候補は GitHub Models だったが、
2026-07-30付けで完全廃止されていた (実機で `gh models run` が410 Goneを確認)。
Cloudflare Workers AI (`@cf/meta/llama-3.1-8b-instruct-fp8-fast`) に切り替えた —
無料枠 (1日10,000ニューロン) に対し1日1回の要約は1%未満、既存のCloudflare
アカウントを流用でき、データ利用ポリシーが学習非使用を明記している (ADR-0009)。

**数値・シグナルはコードが計算し、LLMには言語化のみさせる設計を徹底した。**
警告シグナル (逆イールド・NFCI引き締まり・信用スプレッド/VIXの過去5年パーセンタイル)
とセクターハイライト (下落率が深いことを必須条件に、相対強度プラスまたは
ブレイクアウト検出のどちらかが重なった銘柄) は `signals.ts` がネットワーク不要で
機械的に判定する。LLMは判定結果の言語化だけを行い、**生成テキストに数値・評価語・
マークダウン・開発メモが含まれていれば `sanitizeNote` で破棄する** (#211 の解説
ガードレールと同じ発想)。`pnpm verify` にも同じ検査を独立した経路で追加し
(項目15)、障害注入で実際に落ちることを確認した。

**セクターハイライトは「下落率が深いだけ」を除外する条件で1回作り直した。**
最初は「下落率が深い/相対強度プラス/ブレイクアウト検出の3つのうち2つ以上」と
していたが、実データで検証すると下落率1.42%のVFHのような銘柄まで対象になった
(相対強度+ブレイクアウトの2条件だけで該当)。投資家側の「ただ安いだけを除外したい」
という懸念 (#274 の議論) に立ち返り、**下落率が深いことを必須条件**にしたうえで
残り2条件のどちらかが重なることを要求する形に直した。実データでの該当数は
9銘柄→7銘柄に変わり、いずれも下落率11〜46%の妥当な候補になった。

**LLM呼び出しの失敗は日次バッチ全体を失敗させない設計にした。** `CLOUDFLARE_AI_API_TOKEN`
未設定でも警告/ハイライトの機械判定 (ネットワーク不要) は `summary.json` に保存され、
言語化 (`note`/`economyState`) だけが null のままスキップされる。既存のTiingo等の
コア取得アダプタとは異なり、AI要約は無くても他の指標に影響しない enrichment
レイヤーだと判断した。

**マージ時点でCloudflareのAPIトークンが未設定。** トークン発行はユーザー側の対応が
必要 (Claudeはアカウント作成・認証情報の取得を代行できない)。登録するまでは
「AIによる説明はまだ生成されていない」の表示のまま。

### 景気サイクルの合成コンポジットスコアは見送った (2026-08-23)

投資家×エンジニアの議論の優先度5位。議論自体で「設計の合意形成が必要」と
明記されていた項目で、着手前にユーザーに方針を確認した。

複数指標を1つのスコアに合成する際の重み付けは本質的に人間の価値判断であり、
このプロジェクトが一貫して守ってきた「恣意的な閾値を置かない」原則 (#52) と
緊張する。Conference Board の LEI 構成要素 (景気サイクルの分類根拠、#62) を
等ウェイトで合成する案を提示したが、**公式の重み付けは非公開のため等ウェイトが
「なぜ妥当か」を常に問われ続ける**。ユーザー判断は「合成スコアは作らない」。

**#279 のAI要約 (「経済はどのような状態か」) が同じ役割を部分的に代替している。**
個別指標の値をLLMに渡して傾向を言語化する設計(数値はコードが計算、合成の
重み付けは行わない)は、単一スコアを作らずに「経済状態の一段上の要約」を
提供できている。合成スコアが要るとの結論に至った場合は、改めてIssueを起票する。

### AI要約の実機不具合を修正した (2026-08-23、#281)

`CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_AI_API_TOKEN` をユーザーがローカル `.env` と
GitHub Actions Secrets の両方に登録し、実際にCloudflare Workers AIを呼び出して
動作確認したところ、モックテストでは見つからない2つの不具合が出た。

1. **`max_tokens` を明示していなかったため、複数件 (セクターハイライト7件) を
   まとめて依頼すると応答が既定の出力上限で途中に切れることがあった。** 実測
   3回中1回再現 (最後の項目が文の途中で打ち切られる)
2. **プロンプトに埋め込んだシグナル説明文自体が数字を含んでいた**
   (「下落率が深い方から1/3以内」「過去5年の分布」)。LLMがその数字をそのまま
   説明文に書き写し、`sanitizeNote` の数値ガードレールに毎回引っかかって
   `note` が `null` になる自己矛盾が起きていた

`max_tokens: 1024` を明示し、プロンプトの説明文から数字表現を除去して修正した。
実際のLLM呼び出しを3回連続で行い、途中打ち切り・数値混入が再発しないことを確認した。

**モックしたLLM応答でのテストは「パースロジックが正しいか」しか検証できない。**
「実際のLLMが指示通りの形式・長さで応答するか」は実機でしか分からない。今後
LLM呼び出しを追加・変更する際は、モックテストに加えて実際のAPI呼び出しでの
動作確認を実装完了の条件にする。

**マージ時に日次バッチが `summary.json` を更新しコンフリクトが発生した。**
LLM生成物は `rebuild-views` (#174) のように決定的に再生成できないため、
`git rebase` でコンフリクトした `summary.json` は自分の (修正済みコードで
生成し直した) 版を採用した。

### AI要約ページを初見でも分かるように改善した (2026-08-23、#283)

投資家×エンジニアの議論。`/summary` (#279) が全部プレーンテキストで要点を
スキャンできず、「該当する警告シグナルは無い」のような良い知らせも他の説明文と
同じ見た目で埋もれていた。

**色による善悪判定 (信号機的な赤緑) は既存原則 (#52、評価語を使わない) と衝突するため
追加しない。** 事実の強調で視覚的な階層を作る方向で合意した — 件数バッジ、該当した
条件のチップ (下落率が深い/対SPYで強い/ブレイクアウト検出)、経済状態要約の材料件数。
チップは`SectorHighlight`が既に持つ`hasBreakout`/`relativeStrength`から**コード側で
直接**表示し、新しいデータ計算は追加していない (`signals.ts`は未変更)。

**この議論と実装は、ユーザーから明示的に「先ほどの2人で議論して」と指示されて
行った** — Task 1〜5の議論パターン (投資家視点とエンジニア視点で対立点を出し、
一次情報や既存原則に照らして合意点を見つける) を、機能追加後のUI改善という
別の文脈に転用した実例。

**Tiingoのレート制限に短時間で複数回当たった。** このセッションで daily.yml を
複数回手動実行 (自分の動作確認 + ユーザー自身の確認) した結果、1時間50件の無料枠を
使い切り、workflow_dispatch実行が1件失敗した (#281/#283のコードとは無関係)。
1時間ほどで自然に回復する既知の制約 (ADR-0008)。

### CHoCH/BOSのswingLengthを再較正した (2026-08-23、#285)

ユーザーから「CHoCHとBOSのデータを検証してほしい。精度が悪いし、それぞれが発生
したのか分かりづらい」との指摘。実データで検証したところ、検出された構造の
スパンが半年〜1年半 (全16件のBOSが190〜480日) と異常に長いことが分かった。

**原因は一次情報のデフォルト値 (`swingLength=50`) が日次バーに対して疎すぎる
ことだった。** SPY (2年分501本) で実際にスイングを計算すると、確定するのは
わずか4点 (2025-02-19高値→2025-04-07安値→2026-01-28高値→2026-03-30安値)。
この4点で構造を組むしかないため、検出結果は「1年以上前の安値からの一本調子の
上昇トレンド」を言い換えているだけで、SMC/ICTが意図する局所的な構造変化を
捉えられていなかった。一次情報 (`joshyattridge/smart-money-concepts`) は
Sydney/Tokyo/Londonのキルゾーンのような分足FXトレード向け機能も持ち、
デフォルト値はそちら向けと見られる。

**#268の検証手法 (候補を実データで比較) にスパン長の指標を追加して再検証した。**
#268は「検出数が極端に偏らないか」しか見ておらず、スパン長は見ていなかった —
バックフィル (#249) で2年分のデータが蓄積されたことで、#268時点では見えなかった
問題が顕在化した。5/8/10/12/15/18/20/25/30/50を比較し、検出総数を維持しつつ
(16〜18件、いずれも偏らない) スパンを局所的に保てる最小の候補として`8`を採用。
50→8への変更で、平均スパンが345日→56日、最大477日→94日に縮んだ。

**ユーザーから「一次情報と同じようにして」と言われたときの解釈が難しかった。**
文字どおり一次情報のPythonコードを移植する方向と、一次情報が意図する「局所的な
構造変化を捉える」という目的に忠実になる方向の2通りが考えられた。後者を選んだ
理由: 一次情報の`bos_choch()`関数を全文取得して読み込んだところ、両実装とも
「直近の3点構造」を使う点は同じで、差はスイング点の疎密 (=swingLength) に
起因すると判明したため、コードの移植ではなくパラメータの再較正が本質だった。

### フィラデルフィア連銀業況指数とS&P500前年比を重ねたチャートを追加した (2026-08-23、#287)

ユーザーが参考動画で見たグラフ (フィラデルフィア連銀製造業業況指数を左軸、S&P500の
前年比を右軸で重ねた2軸チャート) と同じ指標を自社データソースから表示したい、
という依頼で着手した。

**両指標とも取得済みで、新規データ取得は不要だった。** フィラデルフィア連銀業況指数
(#94) と S&P500 は既存の指標マスタにあり、`/economy` の「地区連銀サーベイ」チャート
(NY連銀・フィラデルフィア連銀、拡散指数) に、S&P500 の前年同日比 (`yoyAsOf`、#200 で
新設・nasdaq-composite/djia と同じ経路) を右軸で追加する表示変更のみで足りた。

**S&P500 は FRED の制約で直近10年分しか表示できない** (`historyLimit: "10y"`)。
参考動画は2005年〜約20年分だが、ユーザー確認のうえこの制約を許容して進めた。

**`MonthlyChart` は単一軸前提だった。** `index-vs-eps.tsx` の `yAxisId` パターンを
踏襲し、`MonthlySeriesDef` に系列ごとの `axis`/`kind` 上書きを追加して2軸表示に
対応させた。`SharedTooltip` にも `kindOf` (系列ごとの単位を引く関数) を追加したが、
どちらもオプショナルなため既存の呼び出し元 (20件以上) は無変更で動作した
(型チェック・全1400件のテストで確認)。

**worktree からの実機確認で、ブラウザ pane の dev サーバーが main の作業ディレクトリを
指してしまう問題に気付いた。** `preview_start` はセッションの cwd 追従ではなく
固定のプロジェクトディレクトリでサーバーを起動するため、worktree 内の変更が
反映されなかった (実際に見出しが旧タイトルのまま表示された)。worktree 内で
直接 `next dev` を起動し、ブラウザは `navigate` でその URL を直接開くことで確認した。
**worktree で作業する際は、preview_start ではなく手動起動 + navigate を使う。**

### 「対SPY」の起点と算出方法を画面に明記した (2026-08-24、#289)

ユーザーから「AI要約の『対SPY』は期間・算出方法が分かりにくい」との指摘。調査すると
単なる説明不足ではなく**誤読を招く配置**だった — `/market` の下落率チャートの注記で、
下落率の起点 (2024-11-28、Stock Bot 引き継ぎの固定値) を述べた直後に対SPYの説明が
続いており、対SPYも同じ期間だと誤読しやすい構造になっていた。

**実際は対SPYと下落率で起点がまったく違う。** 対SPY (#274) は引き継いだ履歴に価格が
無く計算できないため、Finnhub の生の終値が観測として貯まった日から始まる。
`data/views/drawdown.json` を実測すると、下落率が約1.7年分の見た目に対し
対SPYは実質2.5か月分 (2026-06-08〜) しか無かった。

**固定文言ではなく、ビューから動的に計算する設計にした。** `seedStart` (下落率の起点)
は Stock Bot 引き継ぎの固定日なので画面には静的な文言 (「起点は2024-11-28」) で
問題ないが、対SPYの起点は観測の蓄積とともに前進しうる値のため、ハードコードすると
#135/#170 と同じ「取れないと書いたが実は取れた」型の陳腐化を招く。`buildDrawdownView`
に `relativeStrengthStart` を追加し、**全資産が値を持つ最も遅い日**を動的に算出して
`/market` (対SPYモード選択時のみ表示) と `/summary` の両方に表示した。

**最も早い日ではなく最も遅い日を採る。** 一部の資産だけ値がある日を起点にすると、
まだ値の無い資産まで「この日から出ている」と過大に見せてしまうため。

### シラーPER (CAPE) を追加した (2026-08-24、#291、ADR-0010)

ユーザーから「シラーPERを表示していないか」との確認を受けて着手した。FRED にはシラー
PERが無い (ISM新規受注指数などと同じく著作権のあるデータセット)。

**一次情報は Robert Shiller 本人が公開する `shillerdata.com` の `ie_data.xls`。**
API/JSON経路が無く、旧バイナリ形式のExcelをそのまま配布している。実データを実際に
ダウンロードして構造を確認したところ、`Data`シートの列0=Date・列12=CAPEで、
CAPEは1881-01から (P/E/Dは1871-01からあるが過去10年平均利益が要るため10年遅れる)。

**二次ミラー (multpl.com) を候補から外した。** 実測すると multpl.com は最新の取引日
まで値が出ており、Shiller本人のファイル (月次更新) より高頻度だった。一次情報を
優先する原則 (#65と同じ判断軸) から、定義が公式とズレるリスクのある二次ミラーは
採らず、一次情報のExcelパースを選んだ。

**日付形式の罠を実データで発見した。** 月が1桁のとき小数点以下の0が浮動小数点表現で
落ちる (10月の `2025.10` が `2025.1` になる)。文字列としてそのまま `.` で分割すると
月を取り違える。`toFixed(2)`で2桁に揃えてから読む処理をテストで固定した。

**Excel (旧バイナリ .xls) のパースに `xlsx` (SheetJS CE、Apache-2.0) を新規依存として
追加した。** `exceljs` は旧バイナリ形式を読めないため候補から外した。10個目の
データソースとして ADR-0010 に記録した。

**既存の Forward/実績PER とは重ねず別チャートにした** (#109、#116〜#118で適用済みの
原則)。単年利益 vs 過去10年平均利益で定義が違うため、同じ軸に載せると水準の差が
定義の違いなのか市場の変化なのか判別できなくなる。

**`options.start` (既定10年) に縛られない独自の月次グリッドにした。** CAPEの価値は
145年分の長期文脈にあり、他の系列と同じ10年上限に切り詰めると指標の意義が失われる。
`buildDistribution` (#271) を汎用化 (`ValuationPoint`以外の`{date: string}`点にも
使えるようにジェネリック化) して過去5年分布のパーセンタイル表示も横展開した。

**マージ後、`daily.yml` を手動実行して新しい取得経路が実環境で動くことを確認した**
(CLAUDE.mdの規約: 定期バッチの挙動を変更したら定時実行を待たず手動実行で確認する)。
`pnpm verify` の一次情報照合 (シラーPER分) を含めて成功し、`chore(#9)`のデータ更新
コミットが自動で作られた。ローカル環境では財務省APIへのDNS解決が失敗し`pnpm verify`
をフルには回せなかったが (無関係な既存コード、環境依存の問題)、シラーPER単体の照合
ロジックは別途スクリプトで検証済み (1,748件、不一致0件)。

### Next Action

- **open Issue 2 件** (2026-08-24 時点): #228 (日銀短観、owner-decision のためスキップ中)、
  #262 (Tiingo バックフィル、起票のみ)。#272・#274・#277・#279・#281・#283・#285・#287・#289・#291 は完了
- **複数セッションが同じ Issue に並行着手することがある。** #245 で 2 セッションが同時に
  同じ修正 (`fix/245-...` ブランチ) に着手し、先にマージされた側 (PR #248) が採用された。
  後発側は差分が完全に重複していたためブランチを作り直さず破棄した。作業ディレクトリの共有
  による #240 の教訓と同種で、Issue の assignee 設定だけでは他セッションの並行着手を防げない
  ため、着手前に Issue の assignee・関連 PR の有無を確認する
- `not-in-report` の実データ検証は次に FactSet が発行される週に持ち越し
- **`MAX_TIINGO_FINNHUB_GAP_PERCENT` (verify-values.ts) は暫定値。** Tiingo データが数週間
  蓄積されたら #224 (`MAX_HIGH_GAP_PERCENT`) と同じように実測して見直す
- **Tiingo の欠測は `gaps.json` に記録されない設計のまま。** #229 のセルフレビューで指摘したが
  severity を下げて見送った (`sinceDate` を既存の最新観測日から動的計算する対応で恒久欠測は
  解消済み、記録の仕組み自体は指標マスタの `Gap` 型と設計が合わないため要検討)
- **次の定時実行 (UTC 毎日 02:00) で確認すること**: ブレイクアウト (CHoCH) 検出が
  引き続き動くこと。#262 (バックフィル) が実施されれば、スイング判定に必要な
  データがより早く揃う

### 初回 Finnhub 取得の結果 (2026-08-19、手動実行)

| 項目 | 結果 |
| --- | --- |
| 取得 | **36 銘柄すべて成功**。バッチ緑 |
| 保存キー | **2026-08-18 (月)** = `previousTradingDay(実行日 8/19 火)` |
| 週次グリッドの最終点 | **2026-08-14 (金)** |
| 画面に出るか | **まだ出ない** |

**平日に手動実行すると、取れた値はその週の金曜より後になり画面に出ない。**
`valueAsOf` は過去にしか遡らないため。#125 で gold / QQQ について記録した挙動と同型で、
仕様どおり。定時実行 (UTC 土 02:00 = 米東部の金曜 22 時) なら金曜キーになり即座に乗る。

**GXG は 2 経路が一致したので外した** (#131)。Finnhub が `c=0` を返し、Stock Bot の履歴も
2025-11-22 で途切れていた。取得できないものを残すと週次バッチが恒久的に赤くなり、
**本当の失敗を見落とす**。「緑だが歯抜け」の裏返しで同じくらい危険。
