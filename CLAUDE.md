# macro-shiome (マクロ潮目)

**株式投資の判断に使う指標を 1 つの Web アプリに集約する**。段階を分け、**v1 = バリュエーション**
(S&P 500 / NASDAQ-100 の EPS・PER・イールドスプレッド・予想改定・理論値と割高率)、
**v2 = 市場サマリー** (日経平均・ゴールド・ドル指数・セクター別騰落率など)。
参考にする分析者の記事は転載せず、同じ指標を自分でデータソースから集めて表示する。

## 前提・制約

- **完全無料スタック** (課金しない)。無料枠の制限 (GitHub Actions の実行時間、データソース API のレート制限、Cloudflare Pages のビルド回数) は設計制約として扱う
- **週次バッチ**: GitHub Actions が **UTC 土曜 02:00 (JST 土曜 11:00)** に実行 → データ取得 → **JSON をリポジトリに commit** → push が Cloudflare Pages のビルドをトリガ
- **公開リポジトリ**。NEVER: API キー・トークンをコード / コミット / ログ / クライアントバンドルに残す。秘密情報は GitHub Actions Secrets のみ。**public リポの Actions ログは誰でも読める**
- パッケージマネージャは pnpm

## 技術スタック

SDD Step 2 で確定 (詳細 → `docs/plan.md`、判断の記録 → `docs/adr/`)。

- データストア: **Git 内 JSON** (DB を使わない。ADR-0001)。**Supabase は不採用** — 無料枠が「1 週間の非活動で停止」で週次更新の本アプリと噛み合わないため
- フロント: **Next.js 静的エクスポート** + TypeScript + Tailwind (ADR-0002)
- ホスティング: **Cloudflare Pages** (ADR-0002)
- チャート: **Recharts** (ADR-0003)
- データソース: FRED API / FactSet Earnings Insight 週次 PDF / stockanalysis.com /
  **米財務省 Fiscal Data** (国債入札、ADR-0006) の 4 つ

## 命令

- コミット = `type(#N): 何をしたか [why: 理由]` (#N = 対応 Issue 番号)
- 技術判断 → `docs/adr/` に 1 ファイル。凍結 `inception/` は不可侵、変更は `.agent/decisions.md`
- 技術選定 = 複数案比較で推奨。候補 2 つ以上 / 公式ドキュメントで無料枠・コスト・整合性・運用負荷を確認 / 評価軸を表で / 各案の不適合点を明示
- 原因調査 = 推測禁止、行番号・パス・コマンド出力で示す
- **金融データの定義・計算式は一次情報 (データ提供元の定義) で裏取りする**。EPS / P/E / 益回りの定義や期間 (forward 12M か実績か、暦年か会計年度か) を推測で決めない。指標の出所と定義は ADR かコードコメントに残す
- **指標を追加・変更したら `pnpm verify` を通す** (#113)。実装が動くことではなく**表示している数値が正しいこと**を確かめる。値が間違っていても画面は正常に見えるため、「緑だが検証していない」状態がこのアプリで最も危険
- 末尾 `## STATE` を節目 (着手 / 実装完了 / push / 判断待ち) ごとに更新: (1) 進行中 Issue (#N) とタイトル (2) 進捗 (3) Next Action。進行中 + 直近完了のみ残し、2 世代以上前は `docs/state-archive.md` 先頭へ退避

## 開発フロー (省略禁止)

1. 課題発見 → 実装せず Issue へ
2. Issue 作成 (GitHub Issues、open) → 停止。NEVER: Issue 無し着手
3. ユーザーの着手指示を待つ
4. Issue に assignee を設定 (= In Progress、`gh issue edit N --add-assignee @me`)。NEVER: 指示なし着手
5. feature ブランチで実装 → typecheck / test / build 全グリーン。NEVER: スコープ外混入 / 指示なく別 Issue に着手
6. `/code-review` (Claude セルフレビュー) を diff に実行。Critical/High は即修正、Suggestion は修正 or 理由を記録
7. PR 作成。**1 Issue = 1 PR 厳守**。PR 本文に `closes #N` を記載 (マージで自動クローズ・相互リンク)。NEVER: main 直 push / 1 PR に複数 Issue / `closes #N` 欠落 / PR 本文・コミットに Claude Code 署名 (「Generated with Claude Code」「Co-Authored-By: Claude」)
8. **`/codex-review` は一時中断中** (2026-08-16、ユーザー判断)。PR 作成後は Step 9 のマージへ進む
   <!-- 再開する場合: `npm i -g @openai/codex && codex login` を実行し、以下を戻す。
   レビュー観点は AGENTS.md に、実行スクリプトは scripts/codex-review* に維持してある。
   8. `/codex-review` を実行 (Codex 第三者レビュー)。Codex が PR diff を read-only レビュー
   (観点 = ルートの `AGENTS.md`) → Claude が指摘ごとに修正 or 反論 (理由記録) → 再レビュー、を
   **Codex approve (Critical/High 0 件) に収束するまで自動ループ** (最大 5 ラウンド、超過は停止し
   ユーザー判断)。修正は必ず Claude が行う (Codex はコード不変更) -->
9. 収束 → **Claude が自動マージ** (`gh pr merge --squash --delete-branch`) → 後続 (Issue は `closes #N` で自動クローズ・`## STATE` 更新)
   - **マージ後 CI 確認 (必須、全マージ対象)**: main push で走るワークフローの結果を確認し報告に含める。完了までバックグラウンド監視でよい。失敗時は原因を分類: (1) infra 起因 (runner 停止・ネットワーク・5xx) → `gh run rerun --failed` を **1 回だけ** (2) 実失敗 (型 / テスト / ビルド) → 即修正 or Issue 起票。**rerun も失敗したら原因を再分類して Issue 起票**。NEVER: CI 結果未確認・失敗未報告のままセッション完了
   - docs コミットの扱い: `docs:STATE` を除く **docs コミット (`docs(...)`)** はユーザーが PR diff をレビュー → OK で Claude がマージ
   - **マージ後の完了報告に確認手順を必ず含める (毎回・省略禁止、ユーザーに聞かれる前に書く)**: 「マージ・CI 確認済み」だけで終わらせない。UI / 挙動変更を伴う場合は (1) URL (2) 画面 / 操作手順 (どこをどう操作するか具体的に) (3) 期待挙動 (何が見えるはずか) を書く。バックエンドのみ (UI 変更なし) の場合も「実機確認不要と判断した理由」を一言添える。NEVER: 手順を書かずユーザーに丸投げする
   - NEVER: レビュー前マージ (`/code-review` は省略不可) / 収束前 Done

- Goal 外の問題は触らず Issue 起票のみ (コミット非混入)、完了報告に「新規起票: #N」を併記
- 週次バッチ (GitHub Actions) を変更した場合は、次回の定時実行を待たず `workflow_dispatch` で手動実行して成否を確認し、報告に含める

### GitHub Issues

- 起票先: 本リポの Issues。**起票〜クローズは全て Claude Code (`gh` CLI) が行う**。人手の Web UI 起票は想定しない
- Issue 項目: タイトル (動詞含む) / 背景 / ゴール / 実装方針 (任意) / 受入基準 / 関連 Issue (任意) / 想定 PR タイトル
- ラベル: 種別 `bug`・`feature`・`improvement` + 優先度 `P1` (Urgent)〜`P4` (Low) + `owner-decision` (ユーザー判断待ち)
- 状態: open = Todo / assignee あり = In Progress / closed = Done
- 相互リンク: PR 本文の `closes #N` で自動化 (手動の URL 記載は不要)

### 指標を追加・変更したときの検証 (#113)

`pnpm verify` が機械的に見るのは次の 5 点。**実装と違う経路で照合する**のが要件で、
同じコードを通すと同じ間違いをする (FRED は API キーを使わない CSV 経路)。

1. 一次情報との照合 — 保存値を取り直して突き合わせる
2. 将来日付の混入 — 推計値を含む系列は末尾が未来になる (#100)
3. 最新値の範囲 — 全系列が指標マスタの範囲に収まるか (#102)
4. 時系列の順序と重複
5. 導出値の再計算 — ビュー生成の関数を呼ばずに計算し直す

**機械化できない観点は自分で見る。**

- **画面の説明文と実装の定義が一致しているか**。値が正しくても説明が誤っていることがある
  (#109: NASDAQ-100 表示中に「1 ÷ Forward P/E」と出ていたが実装は実績 PER)
- **系列 ID が一次情報のタイトルどおりか**。FRED のページを開いて確認する
  (#66: `RSCCASN` は小売売上のコントロールグループではなく衣料品店の売上だった)
- **比較している 2 つの値の時点が揃っているか** (#110: 割高率が 1 営業日ずれた終値と比べていた)
- **単位と桁が subtitle の記述と合っているか** (千戸・百万ドル・千件など)

### セルフレビュー観点 (Step 6)

- 変更した関数 / 変数 / 型の呼び出し元を全列挙
- regression リスク 3 つで影響なし確認 (時系列データの欠損・週境界 / タイムゾーン / UPSERT の冪等性 / JSON スキーマ互換 / 導出値の計算式)
- 影響画面を実機確認 (ダッシュボードの各チャート・相関サマリー・期間フィルター)
- テスト 1 件以上 / コメントと実装の乖離なし / try-catch の error 経路を通す
- `/code-review` 省略可: `docs/*.md`・`CLAUDE.md`・`.agent/*` のみ / typo 1 行 / フォーマットのみ。他 (`.ts` `.tsx` `.json` / SQL / CI) は必須
  <!-- 一時中断: `/codex-review` スキップ中のため、ループ記録を完了報告に残すルールは凍結 -->

### SDD

spec + screens → plan → tasks → 実装 (1 タスク = 1 PR)、各ゲートでレビュー。起動は `.claude/commands/` の `/step1-spec` `/step1-screens` `/step2` `/step3` `/step4`。

## STATE (2026-08-17 — **公開済み**。https://macro-shiome.o104085t.workers.dev)

設計フェーズ (SDD Step 1〜3) の経緯は `docs/state-archive.md` に退避済み。

### 完了 (23 件)

| 範囲 | Issue | 要点 |
| --- | --- | --- |
| 基盤 | #1 #2 #23 | Next.js 16 静的エクスポート。**TS 6 / ESLint 9 に意図的に固定**。CI |
| 取得層 | #3 #4 #5 | FRED / FactSet PDF / stockanalysis。**API キーはエラーにも出さない** |
| 検証・導出 | #6 #7 #8 | 1 件でも落ちたら全体失敗。導出値は実測値で検算済み |
| バッチ | #9 #10 | 週次 (UTC 土 02:00) + バックフィル |
| 画面基盤 | #11 #12 | 期間フィルターを URL に載せる。鮮度・欠測表示 |
| チャート | #13〜#20 | バリュエーション 6 種 + サマリーバー + マクロ指標 |
| 規約 | #21 | 利用規約・出所ページ。出所一覧は指標マスタから自動生成 |
| デプロイ | #22 | **Cloudflare Workers Static Assets** に Git 連携で公開 |
| 改善 | #46 #52 #53 #54 | 基準益回りの内訳 / スプレッドの分布 / 欠測理由 / 蓄積中表示 |
| **指標拡充** | **#62〜#66 #72 #83 #84 #87** | 景気サイクルのセクション / 物価 / 住宅 / 労働・所得 / 小売 / LEI 完成 / 信用・商品・為替 / 新規求人 |
| UI/UX | #74〜#77 **#89** | 期間フィルター修正 / モバイル / サマリー / 情報設計 / **問いによる再編** |
| 保守 | **#58 #70** | 指数ラベル・切替 UI を 1 か所に集約 / favicon |
| **記事由来の拡充** | **#93〜#98 #100** | 10年債の理論値 / 地区連銀サーベイ / 家計の余力 / 国債入札 / 国際金利 / 交易条件 |
| 検証層 | **#102** | 範囲チェックが正常値で落ちる / ほとんど動いていなかった問題 |
| **全指標の値検証** | **#109 #110 #113** | 益回りの定義説明の矛盾 / 割高率の比較時点のずれ / **検証の再現可能化** |
| **取得済み指標の活用** | **#115〜#118** | TIPS で実質金利を検算 / 実績 P/E の平均 / 四半期の予想 / 日経平均 |
| **v2 市場サマリー** | **#118 #119** | 日経平均 / ゴールド (ETF 代替)。セクター別騰落率は保留 (#120) |
| 取得の時点 | **#125** | stockanalysis の値を「値が指す日」で保存する |
| **下落率の共通軸** | **#128 #131** | Stock Bot (GAS) の設計を移植。ETF 36 本を最高値からの下落率で横断比較 |

指標 103 件、テスト 438 件。`pnpm verify` で値を検証する (#113)。チャート 30 枚 (`/economy` 19・`/market` 11)。

**指数を増やすときの変更箇所は `src/lib/data/indices.ts` の 1 か所** (#58)。
`INDEX_LABELS` が 5 コンポーネントに複製され、切替 UI も実質 5 か所に散っていた。

### 画面構成 (2026-08-17 時点)

| ページ | 更新頻度 | 内容 |
| --- | --- | --- |
| `/` | 週次 | バリュエーション (指数 vs EPS / Forward P/E / イールドスプレッド / 理論値 / 予想改定 / 相関) |
| `/economy` | 週次 + 月次 | 経済 19 チャート。景気は減速しているか / 雇用と所得は保つか / 消費は続くか / 物価は落ち着くか / 住宅は動いているか |
| `/market` | 週次 + 月次 | 市場 17 チャート。金融環境は緩いか厳しいか / 市場は不安か / いまどの資産が売られているか / 海外市場も同じ方向か |
| `/terms` | — | 利用規約・出所 |

**セクションは「問い」で分ける** (#89)。「先行指標」という見出しでは何に使うかが伝わらず、
指標が 55 件に増えた結果「その他」が最大セクション (旧 `/macro` の 82%) になっていた。
問いにすると「いつ何を見るか」がそのまま伝わり、上流 → 下流の並び順自体が読み方になる。
問いの定義は `src/lib/questions.ts`。

**更新頻度と景気サイクルはチャートのバッジに移した** (#89)。問いに答えるには週次と月次の
両方が要るため、頻度でページを分ける #64 の判断は撤回した。ただし
「先週から動いていない」のか「月次だから動かない」のかは判別できる必要があるため、
頻度をバッジで出す。分類の根拠は The Conference Board の構成要素のまま (#62)。
旧 `/macro` は `public/_redirects` で `/market` へ 301。

### 実装中に判明した重要な点

- **フィクスチャは実データの代わりにならない**。#4 で「ユニットテストは通るのに実 PDF で
  抽出できない」ことが起きた。以降、実データに対する統合テストを `*.integration.test.ts` に残す
- **系列 ID は必ず一次情報で確認する**。#66 で `RSCCASN` を小売売上のコントロールグループ
  として使いかけたが、実際は「**衣料品店の小売売上**」だった。推測で選ぶと別物になる
- **提供元の仕様は実データで確認する** (#53)。FactSet は翌暦年の予想を年央から載せ始める
- **判定は期間フィルター前の系列で行う** (#54)。フィルター後だと誤表示になる
- **長時間ジョブの実行中に PR をマージしない** (#10 のバックフィルで push が reject)
- **ブラウザ pane では画面下部の実機確認ができない**。`document.hidden: true` /
  `innerWidth: 0` で React がハイドレーションを遅延させる。Testing Library で代替する
- **Cloudflare のドキュメントと実 UI は食い違う** (#22)。Pages の Git 連携作成導線が無く、
  実際は Workers Builds に一本化されていた

### データの制約 (画面に出している)

- **NASDAQ-100 の実績 PER は過去に遡れない**。QQQ 経由でしか取れず提供元が現在値のみ
- **EPS はすべて導出値** (指数 ÷ PER)。絶対値を無料で取れる経路が無い
- **住宅ローン申請者数 (MBA) は無料で取れない**。住宅ローン金利で代替 (#65)
- **小売売上のコントロールグループは FRED に無い** (#72)。建材・外食まで除いた系列が存在せず、
  自動車・ガソリンのみ除いた系列で代替。**季節調整済み系列の引き算では導出できない**
- **ISM 新規受注指数は FRED に無い** (ISM の著作権)。LEI の他の新規受注 2 系列で代替
- **中古住宅販売は FRED の提供が 13 点のみ**のため不採用
- **発表ラグは指標ごとに違う**。月次画面は系列単位で対象月を表示する

### 判断の原則 (繰り返し適用している)

- **恣意的な閾値を置かない**。VIX・イールドスプレッド・貯蓄率のいずれも「危険水準」の線を
  引かず、過去分布での位置を示す (#52)
- **根拠の無い分類をしない**。景気サイクルの分類は構成要素に無い指標には付けない (#62)
- **代替指標は「同等でない」と明記する** (#65)

### UI/UX (2026-08-17、#74〜#77 / #89 で改善)

実画面 (375px / 768px / 1280px) で調査し、5 件を修正した。

| 直したこと | 実測 |
| --- | --- |
| 期間フィルターが `/economy` で効かなかった (#74) | 121 点 → 13 点に絞られるようになった |
| モバイルでヘッダーが崩れる (#75) | 370px → **159px**。チャートが 1 画面目に入る |
| 各ページに「最新の状況」を追加 (#76) | 4〜6 チャートをスクロールせず現状が分かる |
| 見出し階層・系列色・欠測表示 (#77) | チャートを h3 に / 色を 1 か所に集約 / 正常な欠測は件数だけ |
| セクションを「問い」で再編 (#89) | 「その他」82% → 消滅。全 24 チャートが 7 つの問いに属する |

**画面の原則** (#89 で更新):
- ページは**対象**で分ける (バリュエーション / 経済 / 市場)
- セクションは**問い**で分ける。問いの中は上流 → 下流の順
- **更新頻度と景気サイクルはチャートのバッジ**で示す (見出しからは外した)
- サマリーは**期間フィルターに依存しない**。常に直近を見せる。`/economy` は**問いごとに 1 指標**
- 月次のカードは**指標ごとに対象月を出す**。発表ラグが違うため

### 記事の探索結果 (2026-08-18)

参考記事 3 本 (債券バブル / リセッション分析 / 週次展望) を読み、未取得の指標を洗い出して
**#93〜#98 を起票**した。FRED / 財務省 API での存在は一次情報で確認済み。

**取れないことを確認したもの** (記録として残す):

- **NAHB 住宅市場指数** — FRED に無い (404、検索でもヒットせず)
- **NAR 仮契約住宅販売指数** — FRED に無い (`PHSI` 404)。NAR の著作権
- **S&P Global PMI (製造業・サービス・総合)** — 著作権で FRED に無い
- 既知: LEI (Conference Board) / MBA 住宅ローン申請 / ISM 新規受注 / 小売コントロールグループ

### 記事由来の拡充 (2026-08-18、#93〜#98)

| Issue | 追加したもの | 判断の要点 |
| --- | --- | --- |
| #93 #100 | **10年債の理論値** = 期待インフレ率 + 潜在成長率 | `GDPPOT` は**将来の推計値を含む**ため取得時に当日以降を切る。潜在成長率は 2.20% (CBO)。**着手前に CSV 末尾を読んで 1.78% と誤認**し #100 で訂正 |
| #94 | 地区連銀サーベイ (NY・フィラデルフィア) | ISM の穴埋め。**当月分が当月中に出る**唯一の月次指標。`unit: 'diffusion'` を新設 (0 を挟むため index の min 0 では弾かれる) |
| #95 | 個人貯蓄額・実質可処分所得の総額 | 率だけでは「取り崩し」か「所得減」か判別できない。実測で貯蓄額 −37.9% / 所得 +0.5% と乖離 |
| #96 | 10年債入札の応札倍率 | **4 つ目のデータソース** (米財務省、ADR-0006)。3 条件で絞らないとリオープンが漏れるか TIPS が混ざる |
| #97 | 日米独の 10年債利回り | 3 か国とも同じ定義 (OECD) に揃える。日次は米国しか無い |
| #98 | 交易条件 (輸入物価 vs 輸出物価) | 物価の連鎖には足さない。上流ではなく別の話のため |

**系列の末尾＝最新とは限らない** (#100)。推計値を含む系列では末尾が将来になる。
値を読むときは日付も一緒に見る。#66 の `RSCCASN`、#96 の TIPS 混入と同じ「別物を掴む」型の失敗。

### 取れないことを確認したもの

- **NAHB 住宅市場指数** — FRED に無い (404、検索でもヒットせず)
- **NAR 仮契約住宅販売指数** — FRED に無い (`PHSI` 404)。NAR の著作権
- **S&P Global PMI (製造業・サービス・総合)** — 著作権で FRED に無い
- 既知: LEI (Conference Board) / MBA 住宅ローン申請 / ISM 新規受注 / 小売コントロールグループ

### 検証層の教訓 (#102)

**「緑だが検証していない」状態が長く続いていた。** 範囲チェックは基準日 (金曜) の値しか
見ておらず、月次・四半期・入札と、発表が 1 週遅れる週次系列 (信用状況) が丸ごと対象外だった。
検証対象を「各系列の最新値」に変えた結果、財政収支・信用状況・応札倍率の 3 つが
**実測値で範囲外**と判明した (負になる指標に `min: 0` が課されていた)。

**単位だけでは自然な範囲が決まらない。** 応札倍率と PER はどちらも `ratio` だが 2 と 20。
指標マスタに `range` の上書きを持たせ、根拠を `note` に書く。

過去の観測をすべて検証すると 7 系列が範囲外になる (1971 年の 357 円、1980 年の FF 金利 20%、
2020 年のマイナス原油など)。**いずれも実在した値**なので範囲は広げず、最新値のみを対象にしている。

### 全指標の値検証 (2026-08-18)

**取得値・導出値は全件一致。** 実装を経由しない独立経路で照合した。

| 対象 | 件数 | 結果 |
| --- | --- | --- |
| FRED 49 系列 × 直近 30 点 (`fredgraph.csv` で取り直し) | 1,470 点 | 不一致 0 |
| 応札倍率 (財務省 API と照合) | 276 件 | 不一致 0 |
| バリュエーション / マクロ / 経済の全導出値 | 約 13,000 値 | 不一致 0 |
| 相関・分布・パーセンタイルの再計算 | — | 一致 |
| 非有限数 (NaN / Infinity) の走査 | 全ビュー | 0 件 |

**見つかったのは「値」ではなく「定義」の問題 2 件**だった。

- **#109**: NASDAQ-100 選択時に「1 ÷ Forward P/E」と「実績 PER」という矛盾する説明が並んでいた。
  実装は実績 PER 由来なので無条件側が誤り。指数ごとに出し分けるよう修正
- **#110**: 割高率が **1 営業日ずれた終値**と比べていた。EPS は FactSet 掲載の終値
  (発行日の前営業日) から作るのに、割高率はレポート日の指数と比較していた。
  実測で最大 2.7pt、直近でも 0.59pt のずれ。`fairValueBasis` を導入して時点を揃えた
  (割高率 3.97% → **3.38%**)

**FactSet の終値はレポート日の前営業日の値**であることを実データで確定した
(FRED の該当日と 0.000% 一致)。抽出は正しく、比較相手だけがずれていた。

### 取得済み指標の活用 (2026-08-18、#115〜#118)

取得しているのに画面にも検証にも使っていない指標が 5 件あった。

| Issue | 対応 | 判断の要点 |
| --- | --- | --- |
| #115 | `dfii10` を実質金利の検算に使う | **表示はしない**。FRED は `T10YIE = DGS10 − DFII10` と定義するため、導出した実質金利は恒等的に TIPS 利回りと一致する (5,909 点で最大差 0.000000)。チャートに出しても同じ線が重なるだけ |
| #116 | 実績 P/E と 5年/10年平均 | Forward と**重ねずに切り替える**。水準が違う (Forward 5年平均 19.9 / 実績 24.4) ので同じ軸だと「PER が上がった」と誤読される |
| #117 | 四半期の予想増益率 | 暦年と**重ねずに切り替える**。暦年は年内固定だが四半期は対象が入れ替わる |
| #118 | 日経平均 | 新しい問い「海外市場も同じ方向か」を作り USD/JPY を移した。S&P 500 とは水準を直接比べられないので指数化はしない (基準日が恣意的になる) |

**「重ねずに切り替える」が 3 回続いた。** 定義や対象が違うものを同じ軸に載せると、差が
「市場の変化」なのか「定義の違い」なのか判別できなくなる。#109 と同じ型の問題。

### v2 の到達点 (2026-08-18)

| 項目 | 結果 |
| --- | --- |
| ドル指数 | #84 で取得済み |
| 日経平均 | #118。新しい問い「海外市場も同じ方向か」を作り USD/JPY を移した |
| ゴールド | **#119。GLD (ETF) で代替**。FRED は 2022年1月に現物価格 (LBMA) の提供を終了 |
| セクター別騰落率 | **保留 (#120)**。FRED に無く 11 本の ETF スクレイピングになる。リクエストが 1 → 12 件 |

**ゴールドは現物価格ではない。** GLD 1 株は金 1/10 オンス相当だが、信託報酬 (年約 0.4%) の分だけ
長期では現物から目減りする。画面に明記している (#65 と同じ扱い)。

### スクレイピング系列の時点 (#125)

**現在値しか取れない系列は「いつの値か」でキーを決める。** 基準日 (金曜) 固定で保存していたため、
手動実行のたびに実行日の値が金曜の値として上書きされていた (実測で確認)。
`previousTradingDay` で実行日の直前の取引日をキーにする。定時実行 (UTC 土曜 02:00 =
米東部の金曜 22 時) なら金曜になり従来と同じ。

**実行日そのものは使えない。** 週次グリッドは金曜から過去に遡るので、土曜の日付で保存すると
金曜の点から見えなくなる。

この修正で **NASDAQ-100 の実績 PER とゴールドは次の定時実行 (土曜) まで表示されない**。
持っている値が月曜のもので、週次グリッドの最終点 (金曜) から遡れないため。
間違った日付の値を出し続けるより無い方を選んだ (#110 と同じ基準)。

### 下落率の共通軸 (2026-08-19、#128)

ユーザーが GAS + スプレッドシートで運用していた **Stock Bot** の設計を移植した。

```
下落率(%) = (最高値 − 現在値) ÷ 最高値 × 100
```

**水準が違う資産を同じ軸に載せるための正規化。** 金 (400 ドル) と S&P 500 ETF (770 ドル) と
原油 ETF (130 ドル) は水準では比べられないが、下落率なら並べられる。#118 で「S&P 500 と
日経平均は水準を直接比べられない」と諦めた問題の解でもある。

- **5 つ目のデータソース = Finnhub** (`Quote`)。ETF は FRED に無く、セクター指数も
  S&P DJI の著作権で取れないため、ここが唯一の無料経路
- ETF **36 本**。主要資産 9 / セクター 8 / コモディティ 7 / 各国 12
- Stock Bot の履歴 (2024-11-29 〜、金曜 89 週) を初期データとして引き継いだ。
  **起点は 2024-11-28 で史上最高値ではない**ことを画面に明記

**最高値はセルに上書きせず、観測から積み上げる導出値にした。** Stock Bot は
`Math.max` でセルを更新していたため、一度誤った高値を掴むと復旧できない。実際に
**VGT が 2026-04-21 に異常値を掴み、以降 85% で固定**されていた (同時期の QQQ は 3.8%)。
観測を全部残す形なら、異常な 1 点を消せば直る。

取り込み時に「**1 週で下落率が 20pt 以上悪化**」を汚染として弾く。逆向き (高い値 → 0) は
最高値の更新なので通す。

**GXG は対象から外した** (#131)。Finnhub が c=0 を返し、Stock Bot の履歴も 2025-11-22 で
途切れている。取得できないものを残すと週次バッチが恒久的に赤くなり、
**本当の失敗を見落とす** (「緑だが歯抜け」の裏返し)。

### Next Action

- **#129 (日本の経済指標: 住宅市場など)** — `owner-decision`、未着手 (ユーザーが「将来的には」と表明)
- **#120 (セクター別騰落率)** — #128 の下落率チャートで実質的に解決 (セクター ETF 8 本)
- `not-in-report` の実データ検証は次に FactSet が発行される週に持ち越し
- **次の定時実行 (UTC 土 02:00 = 2026-08-22) で確認すること**: (1) NASDAQ-100 の実績 PER とゴールドの復旧 (#125) (2) 下落率 36 本が週次グリッドに乗ること (下記)

### 初回 Finnhub 取得の結果 (2026-08-19、手動実行)

| 項目 | 結果 |
| --- | --- |
| 取得 | **36 銘柄すべて成功**。バッチ緑 |
| 保存キー | **2026-08-18 (月)** = `previousTradingDay(実行日 8/19 火)` |
| 週次グリッドの最終点 | **2026-08-14 (金)** |
| 画面に出るか | **まだ出ない** |

**平日に手動実行すると、取れた値はその週の金曜より後になり画面に出ない。**
`valueAsOf` は過去にしか遡らないため。#125 で gold / QQQ について記録した挙動と同型で、
仕様どおり。定時実行 (UTC 土 02:00 = 米東部の金曜 22 時) なら金曜キーになり即座に乗る。

**GXG は 2 経路が一致したので外した** (#131)。Finnhub が `c=0` を返し、Stock Bot の履歴も
2025-11-22 で途切れていた。取得できないものを残すと週次バッチが恒久的に赤くなり、
**本当の失敗を見落とす**。「緑だが歯抜け」の裏返しで同じくらい危険。
